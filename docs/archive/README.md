# Archive

This is the development record, not operational documentation — a historical trail kept
for context on why things are the way they are. For how the project works today, start
from `README.md` and `CLAUDE.md` at the repo root.

- `PROGRESS-diario-completo.md` — the full chronological work diary (pre-2026-08-14);
  the current `PROGRESS.md` at the repo root is a short state summary that points here.
- `AUDIT-2026-07-05.md` — a one-time audit of the codebase; its backlog was cleared on
  2026-07-12 (see `docs/ROADMAP.md`).
- `CLAUDE-sortory-storico.md`, `README-sortory-storico.md`, `PRODUCT.md` — Sortory's own
  docs from when it was a separate app, before the F1-F6 fusion absorbed it into
  Cratory's `/organize` section.
- `superpowers/specs/`, `superpowers/plans/` — the spec and implementation plan for every
  feature built so far (including this documentation review's own), one per feature,
  written for the AI collaborator at the time.
