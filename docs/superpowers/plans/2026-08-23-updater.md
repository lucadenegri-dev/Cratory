# Updater in-place — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cratory scarica, verifica e installa da sé la versione nuova, e riparte — invece di limitarsi ad annunciarla.

**Architecture:** `tauri-plugin-updater` guidato da Rust, non dalla pagina: due comandi (`controlla_aggiornamento`, `installa_aggiornamento`) e un terzo di servizio (`riavvia_app`) incapsulano la sequenza scarica → termina il backend → installa → riavvia, il cui ordine è il punto di tutto il lavoro. Il frontend osserva uno stato e chiede; non orchestra. La release porta tre artefatti in più, prodotti da `tauri build` e pubblicati da uno script nuovo.

**Tech Stack:** Rust + Tauri v2 (`tauri-plugin-updater`, `libc`), TypeScript + React (`@tauri-apps/api`), Python 3.11 (script di pubblicazione, `gh` CLI), pytest, vitest.

Spec: `docs/superpowers/specs/2026-08-23-updater-design.md`.

## Global Constraints

- **Inglese** in `README.md`, `docs/*.md` e `PROGRESS.md`; **italiano** in codice, commenti, nomi di funzione e messaggi di commit.
- **La firma minisign non è la firma Apple.** Non affermare da nessuna parte che l'app è firmata o notarizzata da Apple: non lo è.
- **Non promettere che il dialogo *"Cratory" Not Opened* sparisce** finché il Task 6 non lo ha dimostrato. Fino ad allora nessun documento lo dice.
- **La chiave privata non entra nel repository**, non finisce in un log e non viene stampata da nessuno script.
- **`dangerousInsecureTransportProtocol` non entra mai nel `tauri.conf.json` committato**: vive solo nel file di override locale del Task 6.
- **I tre esiti restano tre**: aggiornato, disponibile, non verificabile. Il terzo non degrada mai nel primo.
- **I codici d'errore sono la fase** (`permessi`, `controllo`, `scaricamento`, `installazione`), non la causa indovinata; le frasi stanno nei dizionari, in **entrambe** le lingue.
- **Ogni asserzione va provata rompendo il codice che sorveglia.** Un test che passa anche con l'implementazione rotta non conta come test.
- Build del bundle: `export PATH="$HOME/.cargo/bin:$PATH"`, poi `python3 src-tauri/scripts/assembla.py`.
- Il design system è monocromo: **non esiste un token `accent`**. I colori disponibili sono `bg/surface/surface-2/elevated/border/border-strong/muted/faint/fg/fg-strong/danger`.

---

### Task 1: La chiave, la configurazione e la build che si rifiuta di partire senza

**Files:**
- Modify: `src-tauri/tauri.conf.json`
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/scripts/assembla.py`
- Test: `backend/tests/test_updater_config.py` (create)

**Interfaces:**
- Produces: `bundle.createUpdaterArtifacts: true` (fa emettere `Cratory.app.tar.gz` + `.sig`), `plugins.updater.pubkey`, `plugins.updater.endpoints`. Il Task 2 registra il plugin che legge questa configurazione; il Task 5 pubblica gli artefatti che nascono da qui.

- [ ] **Step 1: Generare la coppia di chiavi** (azione umana: chiede una password interattiva)

```bash
cd frontend && npx tauri signer generate -w ~/.tauri/cratory.key
```

Produce `~/.tauri/cratory.key` (privata, cifrata con la password) e `~/.tauri/cratory.key.pub`. **Salvare la password nel gestore di password e copiare `cratory.key` fuori da questo Mac.** Se la chiave si perde, nessuna app già installata potrà più aggiornarsi: la pubblica è murata dentro ogni bundle già distribuito, e le firme di una chiave diversa vengono rifiutate.

- [ ] **Step 2: Scrivere i test di guardia sulla configurazione**

Creare `backend/tests/test_updater_config.py`:

```python
"""La configurazione dell'updater è codice: se qualcuno la spegne, va detto.

Questi test non provano il funzionamento dell'updater (per quello serve un
bundle vero, Task 6): provano che la configurazione spedita non possa
degradare in silenzio verso "non aggiorna nulla" o "accetta un manifesto da
chiunque".
"""
import json
from pathlib import Path

RADICE = Path(__file__).resolve().parent.parent.parent
CONF = json.loads((RADICE / "src-tauri" / "tauri.conf.json").read_text())
ENDPOINT = "https://github.com/lucadenegri-dev/Cratory/releases/latest/download/latest.json"


def test_il_bundle_produce_gli_artefatti_dell_updater():
    """Senza questo flag `tauri build` fa solo il .dmg, e la release non ha
    niente che un'app installata possa scaricare."""
    assert CONF["bundle"]["createUpdaterArtifacts"] is True


def test_la_chiave_pubblica_c_e_ed_e_una_chiave():
    pubkey = CONF["plugins"]["updater"]["pubkey"]
    # Il .pub di minisign è una riga base64 di un centinaio di caratteri:
    # un placeholder, una stringa vuota o un percorso non passano di qui.
    assert len(pubkey) > 40
    assert " " not in pubkey
    # `pubkey` vuole il CONTENUTO del .pub, non il percorso del file: un
    # percorso passerebbe il controllo sulla lunghezza e fallirebbe a runtime.
    assert not pubkey.startswith(("~", "/", "."))


def test_l_endpoint_e_quello_della_release_pubblica():
    assert CONF["plugins"]["updater"]["endpoints"] == [ENDPOINT]


def test_niente_http_in_chiaro_nella_configurazione_spedita():
    """L'override per la verifica locale passa da `tauri build --config`. Se
    quel flag finisse nel file committato, l'app spedita accetterebbe un
    manifesto servito da chiunque su http."""
    assert "dangerousInsecureTransportProtocol" not in CONF["plugins"]["updater"]
```

- [ ] **Step 3: Eseguirli e vederli fallire**

```bash
cd backend && source .venv/bin/activate && python -m pytest tests/test_updater_config.py -v
```

Atteso: FAIL — `KeyError: 'createUpdaterArtifacts'` e `KeyError: 'plugins'`.

- [ ] **Step 4: Configurare `tauri.conf.json`**

In `bundle`, accanto a `"active": true`, aggiungere `"createUpdaterArtifacts": true`. In fondo all'oggetto radice, accanto ad `app`, aggiungere:

```json
  "plugins": {
    "updater": {
      "pubkey": "INCOLLARE QUI IL CONTENUTO DI ~/.tauri/cratory.key.pub",
      "endpoints": [
        "https://github.com/lucadenegri-dev/Cratory/releases/latest/download/latest.json"
      ]
    }
  }
```

Il valore di `pubkey` è il **contenuto** del file `.pub` (`cat ~/.tauri/cratory.key.pub`, riga unica, senza le righe di commento `untrusted comment:` se presenti), non un percorso.

- [ ] **Step 5: Rieseguire i test**

```bash
cd backend && source .venv/bin/activate && python -m pytest tests/test_updater_config.py -v
```

Atteso: 4 passed.

- [ ] **Step 6: Aggiungere la dipendenza Rust**

In `src-tauri/Cargo.toml`, sotto `[dependencies]`, dopo `tauri-plugin-opener`:

```toml
# L'updater in-place. La documentazione del plugin suggerisce di vincolarlo ai
# target non mobili; qui sta fra le dipendenze normali perche' Cratory non
# costruisce per Android/iOS -- se un giorno lo facesse, va spostato sotto un
# [target."cfg(not(any(target_os = "android", target_os = "ios")))".dependencies].
tauri-plugin-updater = "2"
```

- [ ] **Step 7: Verificare che compili**

```bash
cd src-tauri && cargo check
```

Atteso: `Finished`. (Prima esecuzione lenta: scarica e compila il plugin.)

- [ ] **Step 8: Scrivere il test della build che si ferma senza chiave**

Aggiungere in fondo a `backend/tests/test_updater_config.py`:

```python
import os
import subprocess
import sys


def test_assembla_si_ferma_subito_senza_chiave_di_firma():
    """Il controllo deve stare PRIMA di tutto il lavoro, non alla fine.

    Il `timeout` è metà dell'asserzione: se il controllo finisse dopo il
    preflight o dopo la build del frontend, questo test non fallirebbe con un
    messaggio — si pianterebbe. Trenta secondi sono un'eternità per un
    controllo su una variabile d'ambiente e infinitamente meno dei ~20 minuti
    di una build vera.
    """
    ambiente = {k: v for k, v in os.environ.items() if k != "TAURI_SIGNING_PRIVATE_KEY"}
    esito = subprocess.run(
        [sys.executable, str(RADICE / "src-tauri" / "scripts" / "assembla.py")],
        capture_output=True, text=True, env=ambiente, timeout=30,
    )
    assert esito.returncode != 0
    assert "TAURI_SIGNING_PRIVATE_KEY" in esito.stdout + esito.stderr
```

- [ ] **Step 9: Eseguirlo e vederlo fallire**

```bash
cd backend && source .venv/bin/activate && python -m pytest tests/test_updater_config.py::test_assembla_si_ferma_subito_senza_chiave_di_firma -v
```

Atteso: FAIL — o per `subprocess.TimeoutExpired` (lo script inizia a costruire), o perché fallisce per un altro motivo senza nominare la variabile.

- [ ] **Step 10: Il controllo in `assembla.py`**

Aggiungere questa funzione accanto alle altre `_`-private, e chiamarla come **prima** istruzione di `main()`, prima del preflight ffmpeg:

```python
def _chiave_di_firma() -> None:
    """La build deve fallire qui, non fra venti minuti al momento del bundling.

    Con `createUpdaterArtifacts` attivo, `tauri build` firma
    `Cratory.app.tar.gz` con la chiave privata: senza, il passo finale fallisce
    dopo aver ricostruito tutto. E un artefatto non firmato non sarebbe
    comunque installabile da nessuna app gia' distribuita, perche' la chiave
    pubblica e' murata dentro i bundle che girano gia'.

    Il valore non viene mai stampato.
    """
    if not os.environ.get("TAURI_SIGNING_PRIVATE_KEY", "").strip():
        raise SystemExit(
            "TAURI_SIGNING_PRIVATE_KEY non e' nell'ambiente: senza chiave privata\n"
            "l'artefatto dell'updater non puo' essere firmato, e la release sarebbe\n"
            "installabile solo a mano. Prima di ricostruire:\n"
            '  export TAURI_SIGNING_PRIVATE_KEY="$(cat ~/.tauri/cratory.key)"\n'
            '  export TAURI_SIGNING_PRIVATE_KEY_PASSWORD="<la password della chiave>"'
        )
```

Aggiornare anche il docstring del modulo: il passo 0 non è più solo il preflight ffmpeg.

- [ ] **Step 11: Rieseguire i test**

```bash
cd backend && source .venv/bin/activate && python -m pytest tests/test_updater_config.py -v
```

Atteso: 5 passed, in pochi secondi.

- [ ] **Step 12: Commit**

```bash
git add src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/scripts/assembla.py backend/tests/test_updater_config.py
git commit -m "feat(updater): chiave di firma, artefatti dell'updater e build che si ferma senza chiave"
```

---

### Task 2: I comandi Rust e la sequenza che non si può sbagliare

**Files:**
- Create: `src-tauri/src/aggiornamento.rs`
- Modify: `src-tauri/src/lib.rs`
- Test: `src-tauri/src/aggiornamento.rs` (modulo `#[cfg(test)]` in coda — primo test Rust del progetto)

**Interfaces:**
- Consumes: la configurazione del Task 1; `backend::termina(&AppHandle)` da `src-tauri/src/backend.rs:661`, già idempotente (`guard.take()`).
- Produces, per il Task 3:
  - comando `controlla_aggiornamento() -> Result<Option<InfoAggiornamento>, ErroreAggiornamento>`
  - comando `installa_aggiornamento() -> Result<(), ErroreAggiornamento>`
  - comando `riavvia_app()`
  - `InfoAggiornamento { versione: String, note: Option<String>, data: Option<String> }`
  - `ErroreAggiornamento { codice: &'static str, dettaglio: String }` con `codice` ∈ `permessi | controllo | scaricamento | installazione`
  - eventi `aggiornamento://progresso` con `{ scaricati: u64, totale: Option<u64> }` e `aggiornamento://installazione` senza payload

- [ ] **Step 1: Scrivere il file con la sola funzione da provare, e il suo test**

Creare `src-tauri/src/aggiornamento.rs`:

```rust
//! L'aggiornamento in-place del guscio desktop.
//!
//! La sequenza (scarica -> termina il backend -> installa -> riavvia) vive
//! qui e non nella pagina: il backend Python gira DA DENTRO il bundle che si
//! sta per sostituire, e un interprete vivo che importa moduli da un `.app`
//! scambiato sotto i piedi fallisce in modi intermittenti e illeggibili.
//! `download` e `install` esistono separati apposta per poter infilare la
//! terminazione in mezzo; `download_and_install`, la scorciatoia documentata,
//! non lascia quello spazio.

use std::path::{Path, PathBuf};

/// La cartella dentro cui l'updater deve poter scrivere per sostituire il
/// bundle. Il binario sta in `Cratory.app/Contents/MacOS/cratory`: tre livelli
/// sopra c'e' il `.app`, e sopra di lui la cartella che lo contiene.
#[cfg(target_os = "macos")]
fn cartella_del_bundle() -> Option<PathBuf> {
    let exe = std::env::current_exe().ok()?;
    let bundle = exe.parent()?.parent()?.parent()?;
    Some(bundle.parent()?.to_path_buf())
}

#[cfg(not(target_os = "macos"))]
fn cartella_del_bundle() -> Option<PathBuf> {
    None
}

/// Scrivibile dall'utente che sta eseguendo l'app.
///
/// Non `Permissions::readonly()`: su Unix quello guarda i bit del proprietario
/// e mente quando il proprietario e' qualcun altro -- una `/Applications` di
/// un Mac gestito, o un volume montato in sola lettura come il `.dmg` da cui
/// qualcuno potrebbe aver lanciato l'app senza copiarla.
#[cfg(unix)]
fn scrivibile(percorso: &Path) -> bool {
    use std::os::unix::ffi::OsStrExt;
    let Ok(c) = std::ffi::CString::new(percorso.as_os_str().as_bytes()) else {
        return false;
    };
    // SAFETY: `c` e' una CString valida e viva per tutta la chiamata;
    // access(2) legge il puntatore e non ne prende possesso.
    unsafe { libc::access(c.as_ptr(), libc::W_OK) == 0 }
}

#[cfg(not(unix))]
fn scrivibile(_percorso: &Path) -> bool {
    true
}

#[cfg(all(test, unix))]
mod test {
    use super::scrivibile;
    use std::fs;
    use std::os::unix::fs::PermissionsExt;

    /// Nota: questi test falliscono se eseguiti da root, per cui W_OK e' vero
    /// ovunque. `cargo test` non va lanciato con sudo.
    #[test]
    fn una_cartella_normale_e_scrivibile() {
        let dir = std::env::temp_dir().join("cratory-scrivibile-si");
        let _ = fs::remove_dir_all(&dir);
        fs::create_dir_all(&dir).unwrap();
        assert!(scrivibile(&dir));
        fs::remove_dir_all(&dir).unwrap();
    }

    #[test]
    fn una_cartella_in_sola_lettura_non_lo_e() {
        let dir = std::env::temp_dir().join("cratory-scrivibile-no");
        let _ = fs::remove_dir_all(&dir);
        fs::create_dir_all(&dir).unwrap();
        fs::set_permissions(&dir, fs::Permissions::from_mode(0o555)).unwrap();
        assert!(!scrivibile(&dir));
        fs::set_permissions(&dir, fs::Permissions::from_mode(0o755)).unwrap();
        fs::remove_dir_all(&dir).unwrap();
    }

    #[test]
    fn un_percorso_inesistente_non_e_scrivibile() {
        assert!(!scrivibile(std::path::Path::new("/cratory-non-esiste/mai")));
    }
}
```

In `src-tauri/src/lib.rs`, in cima, accanto a `mod backend;`:

```rust
mod aggiornamento;
```

- [ ] **Step 2: Eseguire i test e vederli passare, poi provarli rompendo**

```bash
cd src-tauri && cargo test
```

Atteso: 3 passed. Poi verificare che siano veri test: cambiare `libc::W_OK` in `libc::F_OK` e rieseguire — `una_cartella_in_sola_lettura_non_lo_e` deve fallire. Rimettere `W_OK`.

- [ ] **Step 3: I tipi e i due comandi**

Aggiungere in `src-tauri/src/aggiornamento.rs`, **sopra** il modulo `#[cfg(test)]`:

```rust
use serde::Serialize;
use tauri::{AppHandle, Emitter};
use tauri_plugin_updater::{Update, UpdaterExt};

use crate::backend;

pub const EVENTO_PROGRESSO: &str = "aggiornamento://progresso";
pub const EVENTO_INSTALLAZIONE: &str = "aggiornamento://installazione";

#[derive(Serialize, Clone)]
pub struct InfoAggiornamento {
    pub versione: String,
    pub note: Option<String>,
    pub data: Option<String>,
}

#[derive(Serialize, Clone)]
struct Progresso {
    scaricati: u64,
    totale: Option<u64>,
}

/// Il codice e' la FASE in cui si e' fallito, non la causa indovinata: le
/// varianti di `tauri_plugin_updater::Error` non sono un contratto stabile, la
/// fase si'. La frase la scrivono i dizionari del frontend, in due lingue; qui
/// viaggiano solo il codice e il dettaglio tecnico.
#[derive(Serialize)]
pub struct ErroreAggiornamento {
    pub codice: &'static str,
    pub dettaglio: String,
}

impl ErroreAggiornamento {
    fn nuovo(codice: &'static str, dettaglio: impl std::fmt::Display) -> Self {
        Self { codice, dettaglio: dettaglio.to_string() }
    }
}

async fn cerca(app: &AppHandle) -> Result<Option<Update>, ErroreAggiornamento> {
    let updater = app
        .updater()
        .map_err(|e| ErroreAggiornamento::nuovo("controllo", e))?;
    updater
        .check()
        .await
        .map_err(|e| ErroreAggiornamento::nuovo("controllo", e))
}

/// `Ok(None)` e' "sei aggiornato", `Err` e' "non ho potuto controllare".
/// Sono due cose diverse e devono restare tali fino allo schermo.
#[tauri::command]
pub async fn controlla_aggiornamento(
    app: AppHandle,
) -> Result<Option<InfoAggiornamento>, ErroreAggiornamento> {
    Ok(cerca(&app).await?.map(|u| InfoAggiornamento {
        versione: u.version.clone(),
        note: u.body.clone(),
        data: u.date.map(|d| d.to_string()),
    }))
}

#[tauri::command]
pub async fn installa_aggiornamento(app: AppHandle) -> Result<(), ErroreAggiornamento> {
    // Prima di ogni altra cosa: se la cartella che contiene il bundle non e'
    // scrivibile, l'installazione fallirebbe DOPO 172 MB di download inutile.
    if let Some(cartella) = cartella_del_bundle() {
        if !scrivibile(&cartella) {
            return Err(ErroreAggiornamento::nuovo("permessi", cartella.display()));
        }
    }

    // Si ricontrolla: l'`Update` non sopravvive fra una chiamata e l'altra e
    // riprenderlo costa una GET del manifesto. Se nel frattempo non c'e' piu'
    // niente da installare, non si inventa un aggiornamento perche' la pagina
    // ne aveva visto uno un minuto prima.
    let Some(aggiornamento) = cerca(&app).await? else {
        return Ok(());
    };

    let mut scaricati: u64 = 0;
    let eco = app.clone();
    let fine = app.clone();
    let byte = aggiornamento
        .download(
            move |pezzo, totale| {
                scaricati += pezzo as u64;
                let _ = eco.emit(EVENTO_PROGRESSO, Progresso { scaricati, totale });
            },
            move || {
                let _ = fine.emit(EVENTO_INSTALLAZIONE, ());
            },
        )
        .await
        .map_err(|e| ErroreAggiornamento::nuovo("scaricamento", e))?;

    // L'ordine e' la ragione d'essere di questo modulo: il backend muore PRIMA
    // che il bundle da cui sta leggendo venga sostituito.
    backend::termina(&app);

    aggiornamento
        .install(byte)
        .map_err(|e| ErroreAggiornamento::nuovo("installazione", e))?;

    // Su macOS l'installazione non riavvia niente da sola. `restart` non
    // ritorna: la funzione finisce qui.
    app.restart();
}

/// La via d'uscita quando l'installazione fallisce: a quel punto il backend e'
/// gia' stato terminato e l'app e' un guscio vuoto: solo un riavvio la rimette
/// in piedi.
#[tauri::command]
pub fn riavvia_app(app: AppHandle) {
    app.restart();
}
```

Se `cargo build` si lamenta di un valore di ritorno mancante dopo `app.restart()`, in quella versione `restart` non diverge: aggiungere `Ok(())` come ultima riga di `installa_aggiornamento` e nient'altro.

- [ ] **Step 4: Registrare plugin e comandi in `lib.rs`**

Nella catena del `Builder`, dopo `.plugin(tauri_plugin_opener::init())`:

```rust
    // L'updater in-place. La sequenza sta in aggiornamento.rs: qui si registra
    // soltanto il plugin che la esegue.
    .plugin(tauri_plugin_updater::Builder::new().build())
    .invoke_handler(tauri::generate_handler![
      aggiornamento::controlla_aggiornamento,
      aggiornamento::installa_aggiornamento,
      aggiornamento::riavvia_app
    ])
```

- [ ] **Step 5: Compilare ed eseguire tutti i test Rust**

```bash
cd src-tauri && cargo test
```

Atteso: compila senza warning nuovi, 3 passed.

- [ ] **Step 6: Prova sul vivo in sviluppo**

```bash
cd frontend && npm run tauri:dev
```

A finestra aperta, dagli strumenti di sviluppo del webview:

```js
await window.__TAURI_INTERNALS__.invoke("controlla_aggiornamento")
```

Atteso **oggi**, prima che il Task 5 abbia pubblicato il primo manifesto: una `Promise` rifiutata con `{codice: "controllo", dettaglio: "..."}`, perché `releases/latest/download/latest.json` non esiste ancora e risponde 404. È l'esito giusto: "non ho potuto controllare", non "sei aggiornato". Dopo il Task 5 la stessa chiamata restituirà `null` (aggiornato) o l'oggetto della versione nuova.

- [ ] **Step 7: Commit**

```bash
git add src-tauri/src/aggiornamento.rs src-tauri/src/lib.rs
git commit -m "feat(updater): comandi del guscio e sequenza scarica-termina-installa-riavvia"
```

---

### Task 3: Il ponte e il provider

**Files:**
- Create: `frontend/lib/updates-bridge.ts`
- Create: `frontend/lib/updates.tsx`
- Modify: `frontend/package.json` (dipendenza `@tauri-apps/api`)
- Modify: `frontend/app/layout.tsx`
- Test: `frontend/tests/updates-provider.test.tsx` (create)

**Interfaces:**
- Consumes: i tre comandi e i due eventi del Task 2; `isDesktopShell()` da `frontend/lib/external-url.ts:27`.
- Produces, per il Task 4: `useAggiornamento(): { stato, nelGuscio, controllaOra, installaOra, riavviaOra }` con `stato` discriminato su `fase` ∈ `sconosciuto | aggiornato | disponibile | non_verificabile | scaricando | installando | fallito`.

- [ ] **Step 1: Aggiungere la dipendenza**

```bash
cd frontend && npm install @tauri-apps/api@^2
```

Oggi `@tauri-apps/api` c'è solo per transitività di `@tauri-apps/plugin-opener`; `invoke` e `listen` la vogliono diretta.

- [ ] **Step 2: Il ponte**

Creare `frontend/lib/updates-bridge.ts`:

```ts
"use client";

/* L'unico posto che parla col guscio desktop per gli aggiornamenti.
 *
 * Import dinamici come in lib/external-url.ts: nel build per il browser il
 * codice di Tauri non entra nel bundle iniziale e senza guscio non viene
 * nemmeno scaricato. E' un modulo separato anche per una ragione di prova: i
 * test del provider lo sostituiscono in blocco, invece di simulare l'intera
 * API di Tauri. */

export type InfoAggiornamento = { versione: string; note: string | null; data: string | null };
export type ErroreAggiornamento = { codice: string; dettaglio: string };
export type Progresso = { scaricati: number; totale: number | null };

export async function controlla(): Promise<InfoAggiornamento | null> {
  const { invoke } = await import("@tauri-apps/api/core");
  return invoke<InfoAggiornamento | null>("controlla_aggiornamento");
}

export async function installa(): Promise<void> {
  const { invoke } = await import("@tauri-apps/api/core");
  await invoke("installa_aggiornamento");
}

export async function riavvia(): Promise<void> {
  const { invoke } = await import("@tauri-apps/api/core");
  await invoke("riavvia_app");
}

/** Entrambe restituiscono la funzione per smettere di ascoltare. */
export async function ascoltaProgresso(su: (p: Progresso) => void): Promise<() => void> {
  const { listen } = await import("@tauri-apps/api/event");
  return listen<Progresso>("aggiornamento://progresso", (e) => su(e.payload));
}

export async function ascoltaInstallazione(su: () => void): Promise<() => void> {
  const { listen } = await import("@tauri-apps/api/event");
  return listen("aggiornamento://installazione", () => su());
}

/** Un comando Rust che ritorna `Err` rifiuta la promise con l'oggetto
 *  serializzato. Se arriva qualcos'altro (un errore di trasporto, il guscio
 *  che non risponde) non lo si spaccia per un codice che non c'era. */
export function erroreDi(e: unknown): ErroreAggiornamento {
  if (e && typeof e === "object" && "codice" in e && "dettaglio" in e) {
    return e as ErroreAggiornamento;
  }
  return { codice: "sconosciuto", dettaglio: String(e) };
}
```

- [ ] **Step 3: Scrivere i test del provider**

Creare `frontend/tests/updates-provider.test.tsx`:

```tsx
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import { cleanup, render, screen, waitFor } from "@testing-library/react";

// `vi.mock` viene issata sopra gli import: una `const finto` dichiarata qui
// sotto non esisterebbe ancora quando la fabbrica gira. `vi.hoisted` sale
// insieme alle mock ed e' l'unico modo di condividere lo stato con loro.
const finto = vi.hoisted(() => ({
  nelGuscio: true,
  controlla: vi.fn(),
  installa: vi.fn(),
  riavvia: vi.fn(),
}));

vi.mock("@/lib/external-url", () => ({ isDesktopShell: () => finto.nelGuscio }));
vi.mock("@/lib/updates-bridge", async () => {
  const vero = await vi.importActual<typeof import("@/lib/updates-bridge")>("@/lib/updates-bridge");
  return {
    erroreDi: vero.erroreDi,
    controlla: (...a: unknown[]) => finto.controlla(...a),
    installa: (...a: unknown[]) => finto.installa(...a),
    riavvia: (...a: unknown[]) => finto.riavvia(...a),
    ascoltaProgresso: () => Promise.resolve(() => {}),
    ascoltaInstallazione: () => Promise.resolve(() => {}),
  };
});

import { AggiornamentoProvider, useAggiornamento } from "@/lib/updates";

function Sonda() {
  const { stato } = useAggiornamento();
  return <span data-testid="fase">{stato.fase}</span>;
}

const monta = () =>
  render(
    <AggiornamentoProvider>
      <Sonda />
    </AggiornamentoProvider>,
  );

beforeEach(() => {
  finto.nelGuscio = true;
  finto.controlla = vi.fn().mockResolvedValue(null);
  finto.installa = vi.fn().mockResolvedValue(undefined);
  finto.riavvia = vi.fn().mockResolvedValue(undefined);
});
afterEach(cleanup);

describe("provider degli aggiornamenti", () => {
  it("fuori dal guscio non chiede niente a nessuno", async () => {
    finto.nelGuscio = false;
    monta();
    await waitFor(() => expect(screen.getByTestId("fase").textContent).toBe("sconosciuto"));
    expect(finto.controlla).not.toHaveBeenCalled();
  });

  it("all'avvio, nel guscio, controlla una volta sola", async () => {
    monta();
    await waitFor(() => expect(screen.getByTestId("fase").textContent).toBe("aggiornato"));
    expect(finto.controlla).toHaveBeenCalledTimes(1);
  });

  it("una versione nuova diventa 'disponibile'", async () => {
    finto.controlla = vi.fn().mockResolvedValue({ versione: "1.0.4", note: "note", data: null });
    monta();
    await waitFor(() => expect(screen.getByTestId("fase").textContent).toBe("disponibile"));
  });

  it("un controllo fallito non diventa mai 'aggiornato'", async () => {
    finto.controlla = vi.fn().mockRejectedValue({ codice: "controllo", dettaglio: "404" });
    monta();
    await waitFor(() => expect(screen.getByTestId("fase").textContent).toBe("non_verificabile"));
  });
});
```

- [ ] **Step 4: Eseguirli e vederli fallire**

```bash
cd frontend && npx vitest run tests/updates-provider.test.tsx
```

Atteso: FAIL — `Failed to resolve import "@/lib/updates"`.

- [ ] **Step 5: Il provider**

Creare `frontend/lib/updates.tsx`:

```tsx
"use client";

/* Lo stato dell'aggiornamento, in un posto solo.
 *
 * Fuori dal guscio desktop non fa nulla e non chiede nulla: nel browser non
 * c'e' niente da installare. Dentro, controlla una volta per lancio e
 * fallisce in silenzio -- un problema di rete all'avvio non e' una notizia da
 * dare a chi non ha chiesto niente. */

import { createContext, useCallback, useContext, useEffect, useMemo, useRef, useState, type ReactNode } from "react";
import { isDesktopShell } from "@/lib/external-url";
import {
  ascoltaInstallazione,
  ascoltaProgresso,
  controlla,
  erroreDi,
  installa,
  riavvia,
  type ErroreAggiornamento,
  type InfoAggiornamento,
} from "@/lib/updates-bridge";

export type StatoAggiornamento =
  | { fase: "sconosciuto" }
  | { fase: "aggiornato" }
  | { fase: "disponibile"; info: InfoAggiornamento }
  | { fase: "non_verificabile"; errore: ErroreAggiornamento }
  | { fase: "scaricando"; info: InfoAggiornamento; scaricati: number; totale: number | null }
  | { fase: "installando"; info: InfoAggiornamento }
  | { fase: "fallito"; info: InfoAggiornamento | null; errore: ErroreAggiornamento };

type Contesto = {
  stato: StatoAggiornamento;
  /** Solo qui c'e' qualcosa da installare. */
  nelGuscio: boolean;
  controllaOra: () => Promise<void>;
  installaOra: () => Promise<void>;
  riavviaOra: () => Promise<void>;
};

const ContestoAggiornamento = createContext<Contesto>({
  stato: { fase: "sconosciuto" },
  nelGuscio: false,
  controllaOra: async () => {},
  installaOra: async () => {},
  riavviaOra: async () => {},
});

export function useAggiornamento(): Contesto {
  return useContext(ContestoAggiornamento);
}

export function AggiornamentoProvider({ children }: { children: ReactNode }) {
  const [stato, setStato] = useState<StatoAggiornamento>({ fase: "sconosciuto" });
  const [nelGuscio, setNelGuscio] = useState(false);
  // Lo stato serve anche a chi non sta rendendo (installaOra): leggerlo da
  // dentro un updater di setStato per portarselo fuori renderebbe impuro
  // l'updater, che React ha il diritto di eseguire due volte.
  const rifStato = useRef<StatoAggiornamento>({ fase: "sconosciuto" });
  useEffect(() => {
    rifStato.current = stato;
  }, [stato]);

  const controllaOra = useCallback(async () => {
    try {
      const info = await controlla();
      setStato(info ? { fase: "disponibile", info } : { fase: "aggiornato" });
    } catch (e) {
      setStato({ fase: "non_verificabile", errore: erroreDi(e) });
    }
  }, []);

  useEffect(() => {
    if (!isDesktopShell()) return;
    setNelGuscio(true);
    void controllaOra();
  }, [controllaOra]);

  const installaOra = useCallback(async () => {
    const attuale = rifStato.current;
    if (attuale.fase !== "disponibile") return;
    const info: InfoAggiornamento = attuale.info;
    setStato({ fase: "scaricando", info, scaricati: 0, totale: null });
    const smetti: Array<() => void> = [];
    try {
      smetti.push(
        await ascoltaProgresso((p) =>
          setStato((s) => (s.fase === "scaricando" ? { ...s, scaricati: p.scaricati, totale: p.totale } : s)),
        ),
      );
      smetti.push(
        await ascoltaInstallazione(() =>
          setStato((s) => (s.fase === "scaricando" ? { fase: "installando", info: s.info } : s)),
        ),
      );
      await installa();
      // Se la promessa si risolve, l'app non si e' riavviata: l'installazione
      // e' comunque avvenuta, e il riavvio resta da fare.
      setStato((s) => (s.fase === "scaricando" || s.fase === "installando" ? { fase: "installando", info: s.info } : s));
    } catch (e) {
      setStato({ fase: "fallito", info, errore: erroreDi(e) });
    } finally {
      smetti.forEach((f) => f());
    }
  }, []);

  const riavviaOra = useCallback(async () => {
    await riavvia();
  }, []);

  const valore = useMemo(
    () => ({ stato, nelGuscio, controllaOra, installaOra, riavviaOra }),
    [stato, nelGuscio, controllaOra, installaOra, riavviaOra],
  );

  return <ContestoAggiornamento.Provider value={valore}>{children}</ContestoAggiornamento.Provider>;
}
```

- [ ] **Step 6: Rieseguire i test**

```bash
cd frontend && npx vitest run tests/updates-provider.test.tsx
```

Atteso: 4 passed. Poi provarli: in `controllaOra` sostituire il `catch` con `setStato({ fase: "aggiornato" })` e verificare che *"un controllo fallito non diventa mai 'aggiornato'"* fallisca. Rimettere.

- [ ] **Step 7: Montarlo nel layout**

In `frontend/app/layout.tsx`, importare `AggiornamentoProvider` da `@/lib/updates` e avvolgere il contenuto **dentro** `I18nProvider` (le frasi d'errore sono tradotte) e **fuori** da `ShellSwitch` (la nav lo legge):

```tsx
        <I18nProvider>
          <AggiornamentoProvider>
            <PlayerProvider>
              <SetupGate />
              <ShellSwitch>{children}</ShellSwitch>
              <DockedPlayer />
            </PlayerProvider>
          </AggiornamentoProvider>
        </I18nProvider>
```

- [ ] **Step 8: Verificare che il build regga**

```bash
cd frontend && npm run lint && npm run build
```

Atteso: nessun errore. Il provider non deve rompere l'export statico: non tocca `window` fuori da `useEffect`.

- [ ] **Step 9: Commit**

```bash
git add frontend/lib/updates-bridge.ts frontend/lib/updates.tsx frontend/app/layout.tsx frontend/package.json frontend/package-lock.json frontend/tests/updates-provider.test.tsx
git commit -m "feat(updater): ponte col guscio e provider dello stato di aggiornamento"
```

---

### Task 4: L'avviso nella nav e la card che aggiorna

**Files:**
- Create: `frontend/components/settings/aggiornamento-guscio.tsx`
- Create: `frontend/components/settings/aggiornamento-browser.tsx`
- Modify: `frontend/components/settings/version-card.tsx`
- Modify: `frontend/components/index-nav.tsx`
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts`
- Test: `frontend/tests/aggiornamento-guscio.test.tsx` (create)

**Interfaces:**
- Consumes: `useAggiornamento()` dal Task 3; `getAppVersion` e `checkUpdates` da `@/lib/api`, invariati.
- Produces: `<AggiornamentoGuscio versione={string} />` e `<AggiornamentoBrowser versione={string} />` — due modi alternativi che si spartiscono la stessa scheda. Ognuno rende la propria riga di intestazione (versione a sinistra, bottone a destra) e il proprio corpo; `VersionCard` legge la versione una volta sola e sceglie quale dei due montare.

- [ ] **Step 1: Le chiavi i18n**

In `frontend/lib/i18n/en.ts`, dentro `settings`, dopo `versionNotesHeading`:

```ts
    versionInstall: "Download and install (≈172 MB)",
    versionConfirmTitle: "Update Cratory?",
    versionConfirmBody:
      "About 172 MB will be downloaded, the app will close and start again on its own, and anything running right now — an analysis, a download — is interrupted.",
    versionDownloading: (fatti: string, totale: string) => `Downloading ${fatti} MB of ${totale} MB`,
    versionDownloadingUnknown: (fatti: string) => `Downloading ${fatti} MB`,
    versionInstalling: "Installing. The app will start again on its own.",
    versionRestart: "Restart the app",
    versionErrPermessi: "Cratory cannot write to the folder it is installed in. Install the new version by hand.",
    versionErrControllo: "Could not check for updates.",
    versionErrScaricamento: "The download failed, or the package is not authentic.",
    versionErrInstallazione: "The installation failed. Restart the app, then install the new version by hand.",
    versionErrSconosciuto: "The update did not go through.",
```

In `frontend/lib/i18n/it.ts`, nello stesso punto:

```ts
    versionInstall: "Scarica e installa (≈172 MB)",
    versionConfirmTitle: "Aggiornare Cratory?",
    versionConfirmBody:
      "Si scaricano circa 172 MB, l'app si chiude e riparte da sola, e quello che è in corso adesso — un'analisi, un download — viene interrotto.",
    versionDownloading: (fatti: string, totale: string) => `Scaricati ${fatti} MB di ${totale} MB`,
    versionDownloadingUnknown: (fatti: string) => `Scaricati ${fatti} MB`,
    versionInstalling: "Installazione in corso. L'app riparte da sola.",
    versionRestart: "Riavvia l'app",
    versionErrPermessi: "Cratory non può scrivere nella cartella in cui è installato. Installa la versione nuova a mano.",
    versionErrControllo: "Non è stato possibile controllare gli aggiornamenti.",
    versionErrScaricamento: "Il download non è riuscito, oppure il pacchetto non è autentico.",
    versionErrInstallazione: "L'installazione non è riuscita. Riavvia l'app, poi installa la versione nuova a mano.",
    versionErrSconosciuto: "L'aggiornamento non è andato a buon fine.",
```

In `en.ts` e `it.ts`, dentro `nav`, aggiungere rispettivamente `updateAvailable: "An update is available",` e `updateAvailable: "È disponibile un aggiornamento",`.

- [ ] **Step 2: Scrivere i test della scheda**

Creare `frontend/tests/aggiornamento-guscio.test.tsx`:

```tsx
import { afterEach, describe, expect, it, vi } from "vitest";
import { cleanup, fireEvent, render, screen } from "@testing-library/react";
import type { StatoAggiornamento } from "@/lib/updates";

// `vi.hoisted`: la fabbrica di `vi.mock` gira prima delle dichiarazioni del
// modulo, quindi lo stato condiviso deve salire insieme a lei.
const finto = vi.hoisted(() => ({
  stato: { fase: "sconosciuto" } as StatoAggiornamento,
  controllaOra: vi.fn(),
  installaOra: vi.fn(),
  riavviaOra: vi.fn(),
}));

vi.mock("@/lib/updates", () => ({
  useAggiornamento: () => ({ ...finto, nelGuscio: true }),
}));

import { AggiornamentoGuscio } from "@/components/settings/aggiornamento-guscio";

afterEach(cleanup);

const monta = () => render(<AggiornamentoGuscio versione="Stai usando la 1.0.3" />);

describe("scheda aggiornamento nel guscio", () => {
  it("con una versione nuova offre l'installazione, e il clic chiede conferma prima", () => {
    finto.stato = { fase: "disponibile", info: { versione: "1.0.4", note: "note vere", data: null } };
    monta();
    expect(screen.getByText("note vere")).toBeTruthy();
    fireEvent.click(screen.getByText(/Scarica e installa/));
    // La conferma dice le tre cose vere; l'installazione non è ancora partita.
    expect(screen.getByText(/viene interrotto/)).toBeTruthy();
    expect(finto.installaOra).not.toHaveBeenCalled();
  });

  it("mostra i MB durante il download, e non lascia ripremere il controllo", () => {
    finto.stato = {
      fase: "scaricando",
      info: { versione: "1.0.4", note: null, data: null },
      scaricati: 90_000_000,
      totale: 180_000_000,
    };
    monta();
    expect(screen.getByText("Scaricati 90 MB di 180 MB")).toBeTruthy();
    expect(screen.getByText("Controlla aggiornamenti").closest("button")?.disabled).toBe(true);
  });

  it("dopo un fallimento offre il riavvio, perché il backend è già stato terminato", () => {
    finto.stato = {
      fase: "fallito",
      info: { versione: "1.0.4", note: null, data: null },
      errore: { codice: "installazione", dettaglio: "read-only file system" },
    };
    monta();
    expect(screen.getByText(/L'installazione non è riuscita/)).toBeTruthy();
    expect(screen.getByText("read-only file system")).toBeTruthy();
    fireEvent.click(screen.getByText("Riavvia l'app"));
    expect(finto.riavviaOra).toHaveBeenCalled();
  });

  it("un controllo fallito non dice che sei aggiornato", () => {
    finto.stato = { fase: "non_verificabile", errore: { codice: "controllo", dettaglio: "404" } };
    monta();
    expect(screen.getByText(/Non è stato possibile controllare/)).toBeTruthy();
    expect(screen.queryByText(/all'ultima versione/)).toBeNull();
  });

  it("mostra sempre la versione in uso, in ogni fase", () => {
    finto.stato = { fase: "installando", info: { versione: "1.0.4", note: null, data: null } };
    monta();
    expect(screen.getByText("Stai usando la 1.0.3")).toBeTruthy();
  });
});
```

I MB sono in megabyte decimali (1.000.000), come li conta chiunque guardi la dimensione di un file su macOS: 90.000.000 byte sono 90 MB, non 86 MiB.

- [ ] **Step 3: Eseguirli e vederli fallire**

```bash
cd frontend && npx vitest run tests/aggiornamento-guscio.test.tsx
```

Atteso: FAIL — `Failed to resolve import "@/components/settings/aggiornamento-guscio"`.

- [ ] **Step 4: La scheda del guscio**

Creare `frontend/components/settings/aggiornamento-guscio.tsx`:

```tsx
"use client";

/* Il flusso di aggiornamento dentro il guscio desktop: qui c'e' qualcosa da
   installare, e questo componente e' l'unico posto che lo fa fare. La versione
   in uso arriva come prop perche' la legge VersionCard, una volta sola, per
   entrambi i modi. */

import { useState } from "react";
import { ExternalLink } from "lucide-react";
import { Button } from "@/components/ui";
import { ConfirmModal } from "@/components/confirm-modal";
import { useT, type Dictionary } from "@/lib/i18n";
import { useAggiornamento } from "@/lib/updates";

const MB = 1_000_000;
const mb = (byte: number) => Math.round(byte / MB).toString();

/** La frase la sceglie il dizionario a partire dal codice di fase: il guscio
 *  manda il codice, non la prosa. */
function frase(t: Dictionary, codice: string): string {
  switch (codice) {
    case "permessi": return t.settings.versionErrPermessi;
    case "controllo": return t.settings.versionErrControllo;
    case "scaricamento": return t.settings.versionErrScaricamento;
    case "installazione": return t.settings.versionErrInstallazione;
    default: return t.settings.versionErrSconosciuto;
  }
}

export function AggiornamentoGuscio({ versione }: { versione: string }) {
  const t = useT();
  const { stato, controllaOra, installaOra, riavviaOra } = useAggiornamento();
  const [conferma, setConferma] = useState(false);
  // Durante download e installazione non si ricontrolla: il controllo
  // sostituirebbe lo stato da cui dipende cio' che sta gia' succedendo.
  const occupato = stato.fase === "scaricando" || stato.fase === "installando";

  const corpo = () => {
    switch (stato.fase) {
      case "aggiornato":
        return <p className="mt-3 text-xs text-muted">{t.settings.versionUpToDate}</p>;

      case "non_verificabile":
        return (
          <p className="mt-3 text-xs text-danger">
            {frase(t, stato.errore.codice)}{" "}
            <span className="text-faint">{stato.errore.dettaglio}</span>
          </p>
        );

      case "scaricando":
        return (
          <p className="tnum mt-3 text-xs text-muted">
            {stato.totale
              ? t.settings.versionDownloading(mb(stato.scaricati), mb(stato.totale))
              : t.settings.versionDownloadingUnknown(mb(stato.scaricati))}
          </p>
        );

      case "installando":
        return <p className="mt-3 text-xs text-muted">{t.settings.versionInstalling}</p>;

      case "fallito":
        return (
          <div className="mt-3 border-t border-border pt-3">
            <p className="text-xs text-danger">{frase(t, stato.errore.codice)}</p>
            <p className="mt-1 text-[10px] text-faint">{stato.errore.dettaglio}</p>
            {/* A questo punto il backend e' gia' stato terminato: senza un
                riavvio l'app resta un guscio vuoto. */}
            <Button className="mt-2" size="sm" variant="outline" onClick={() => void riavviaOra()}>
              {t.settings.versionRestart}
            </Button>
          </div>
        );

      case "disponibile":
        return (
          <div className="mt-3 border-t border-border pt-3">
            <p className="text-sm text-fg-strong">{t.settings.versionAvailable(stato.info.versione)}</p>
            {stato.info.note && (
              <>
                <p className="mt-2 text-[10px] uppercase tracking-wider text-faint">
                  {t.settings.versionNotesHeading}
                </p>
                {/* Testo di GitHub, non nostro: si mostra come citazione. */}
                <pre className="mt-1 max-h-40 overflow-auto whitespace-pre-wrap border-l-2 border-border pl-3 text-xs text-muted">
                  {stato.info.note}
                </pre>
              </>
            )}
            <div className="mt-2 flex flex-wrap gap-2">
              <Button size="sm" variant="primary" onClick={() => setConferma(true)}>
                {t.settings.versionInstall}
              </Button>
              {/* La via d'uscita quando l'automatismo non funziona. */}
              <a
                href={`https://github.com/lucadenegri-dev/Cratory/releases/tag/v${stato.info.versione}`}
                target="_blank"
                rel="noreferrer"
              >
                <Button size="sm" variant="outline">
                  <ExternalLink size={14} /> {t.settings.versionOpenRelease}
                </Button>
              </a>
            </div>
          </div>
        );

      default:
        return null;
    }
  };

  return (
    <>
      <div className="flex flex-wrap items-center justify-between gap-3">
        <span className="text-sm text-fg">{versione}</span>
        <Button size="sm" variant="outline" disabled={occupato} onClick={() => void controllaOra()}>
          {t.settings.versionCheck}
        </Button>
      </div>
      {corpo()}
      <ConfirmModal
        open={conferma}
        title={t.settings.versionConfirmTitle}
        message={t.settings.versionConfirmBody}
        confirmLabel={t.settings.versionInstall}
        onClose={() => setConferma(false)}
        onConfirm={() => {
          setConferma(false);
          void installaOra();
        }}
      />
    </>
  );
}
```

- [ ] **Step 5: Rieseguire i test**

```bash
cd frontend && npx vitest run tests/aggiornamento-guscio.test.tsx
```

Atteso: 5 passed. Poi provarli: togliere `disabled={occupato}` e verificare che il secondo test fallisca; far restituire `t.settings.versionUpToDate` al caso `non_verificabile` e verificare che fallisca il quarto. Rimettere.

- [ ] **Step 6: La scheda del browser, presa da com'è oggi**

Creare `frontend/components/settings/aggiornamento-browser.tsx` col contenuto attuale di `version-card.tsx`, con tre sole differenze: il nome del componente, la versione che arriva come prop invece di essere letta qui, e nessun bordo esterno (lo mette `VersionCard`).

```tsx
"use client";

/* Il controllo aggiornamenti fuori dal guscio desktop: nel browser di sviluppo
   non c'e' niente da installare, quindi resta il bottone che interroga il
   backend e i tre esiti. Tre e restano tre: aggiornato, disponibile, non
   verificabile — il terzo ha il suo posto e il suo tono. */

import { useState } from "react";
import { ExternalLink } from "lucide-react";
import { checkUpdates, errText, type UpdateCheckResult } from "@/lib/api";
import { Button } from "@/components/ui";
import { useT } from "@/lib/i18n";

export function AggiornamentoBrowser({ versione }: { versione: string }) {
  const t = useT();
  const [esito, setEsito] = useState<UpdateCheckResult | null>(null);
  const [errore, setErrore] = useState<string | null>(null);
  const [inCorso, setInCorso] = useState(false);

  const controlla = async () => {
    setInCorso(true);
    setErrore(null);
    setEsito(null);
    try {
      setEsito(await checkUpdates());
    } catch (e) {
      setErrore(errText(e));
    } finally {
      setInCorso(false);
    }
  };

  return (
    <>
      <div className="flex flex-wrap items-center justify-between gap-3">
        <span className="text-sm text-fg">{versione}</span>
        <Button size="sm" variant="outline" disabled={inCorso} onClick={controlla}>
          {inCorso ? t.settings.versionChecking : t.settings.versionCheck}
        </Button>
      </div>

      {errore && <p className="mt-3 text-xs text-danger">{errore}</p>}

      {esito && !esito.update_available && (
        <p className="mt-3 text-xs text-muted">{t.settings.versionUpToDate}</p>
      )}

      {esito?.update_available && (
        <div className="mt-3 border-t border-border pt-3">
          <p className="text-sm text-fg-strong">{t.settings.versionAvailable(esito.latest ?? "")}</p>
          {esito.notes && (
            <>
              <p className="mt-2 text-[10px] uppercase tracking-wider text-faint">
                {t.settings.versionNotesHeading}
              </p>
              {/* Testo di GitHub, non nostro: si mostra come citazione. */}
              <pre className="mt-1 max-h-40 overflow-auto whitespace-pre-wrap border-l-2 border-border pl-3 text-xs text-muted">
                {esito.notes}
              </pre>
            </>
          )}
          {esito.url && (
            <a href={esito.url} target="_blank" rel="noreferrer" className="mt-2 inline-block">
              <Button size="sm" variant="outline">
                <ExternalLink size={14} /> {t.settings.versionOpenRelease}
              </Button>
            </a>
          )}
        </div>
      )}
    </>
  );
}
```

Sostituire poi **tutto** il contenuto di `frontend/components/settings/version-card.tsx` con:

```tsx
"use client";

import { useEffect, useState } from "react";
import { getAppVersion } from "@/lib/api";
import { useT } from "@/lib/i18n";
import { useAggiornamento } from "@/lib/updates";
import { AggiornamentoBrowser } from "./aggiornamento-browser";
import { AggiornamentoGuscio } from "./aggiornamento-guscio";

/* La versione in uso, e chi si occupa degli aggiornamenti.

   Due mondi, non due rami sparsi: nel guscio desktop c'e' qualcosa da
   installare e se ne occupa AggiornamentoGuscio; nel browser di sviluppo non
   c'e', e resta il bottone che interroga il backend. La versione la legge
   questo componente, una volta sola, e la passa a chi dei due viene montato. */
export function VersionCard() {
  const t = useT();
  const [versione, setVersione] = useState<string | null>(null);
  const { nelGuscio } = useAggiornamento();

  useEffect(() => {
    getAppVersion().then((r) => setVersione(r.version)).catch(() => setVersione(null));
  }, []);

  const etichetta = versione ? t.settings.versionCurrent(versione) : "—";

  return (
    <div className="border border-border p-5">
      {nelGuscio ? (
        <AggiornamentoGuscio versione={etichetta} />
      ) : (
        <AggiornamentoBrowser versione={etichetta} />
      )}
    </div>
  );
}
```

- [ ] **Step 7: Il punto nella nav**

In `frontend/components/index-nav.tsx`, importare `useAggiornamento` da `@/lib/updates`, prendere `const { stato } = useAggiornamento();` accanto agli altri hook del componente, e dentro il `<Link href="/settings">` del piede, subito dopo `{t.nav.settings}`:

```tsx
          {stato.fase === "disponibile" && (
            <span
              aria-label={t.nav.updateAvailable}
              title={t.nav.updateAvailable}
              className="ml-auto h-1.5 w-1.5 shrink-0 rounded-full bg-fg-strong"
            />
          )}
```

`bg-fg-strong` e non un colore d'accento: il design system è monocromo e un token `accent` non esiste (`frontend/app/globals.css`, blocco `@theme inline`).

- [ ] **Step 8: Tutta la suite frontend**

```bash
cd frontend && npm run lint && npm run test:unit && npm run build
```

Atteso: lint pulito, tutti i test verdi (compresi i due file nuovi), build completata. Se `tsc` si lamenta di chiavi mancanti in `it.ts`, è il tipo `Dictionary` che fa il suo lavoro: manca una traduzione.

- [ ] **Step 9: Commit**

```bash
git add frontend/components frontend/lib/i18n frontend/tests
git commit -m "feat(updater): avviso nella nav e installazione dalla scheda versione"
```

---

### Task 5: Il manifesto e la pubblicazione

**Files:**
- Create: `src-tauri/scripts/pubblica.py`
- Test: `backend/tests/test_manifest_updater.py` (create)

**Interfaces:**
- Consumes: gli artefatti prodotti dal Task 1 (`src-tauri/target/release/bundle/macos/Cratory.app.tar.gz` e `.sig`, `.../bundle/dmg/Cratory_<versione>_aarch64.dmg`).
- Produces: `costruisci_manifest(versione, note, firma, pub_date, base_url=None) -> dict`, usata anche dal Task 6 per il manifesto locale.

- [ ] **Step 1: Scrivere i test della funzione pura**

Creare `backend/tests/test_manifest_updater.py`:

```python
"""Il manifesto che l'updater legge. Sei modi di sbagliarlo, tutti silenziosi:
un manifesto malformato non fa rumore, semplicemente nessuno si aggiorna piu'.
"""
import importlib.util
from pathlib import Path

import pytest

RADICE = Path(__file__).resolve().parent.parent.parent
_spec = importlib.util.spec_from_file_location(
    "pubblica", RADICE / "src-tauri" / "scripts" / "pubblica.py"
)
pubblica = importlib.util.module_from_spec(_spec)
_spec.loader.exec_module(pubblica)

FIRMA = "dW50cnVzdGVkIGNvbW1lbnQ6IHNpZ25hdHVyZQpSVQ==\n"
QUANDO = "2026-08-23T10:00:00Z"


def manifesto(**extra):
    argomenti = dict(versione="1.0.3", note="Note.", firma=FIRMA, pub_date=QUANDO)
    argomenti.update(extra)
    return pubblica.costruisci_manifest(**argomenti)


def test_la_firma_e_il_contenuto_del_sig_non_un_percorso():
    piattaforma = manifesto()["platforms"]["darwin-aarch64"]
    assert piattaforma["signature"] == FIRMA.strip()


def test_l_url_punta_all_asset_di_quel_tag_non_a_latest():
    url = manifesto()["platforms"]["darwin-aarch64"]["url"]
    assert url == (
        "https://github.com/lucadenegri-dev/Cratory/releases/download/"
        "v1.0.3/Cratory.app.tar.gz"
    )
    assert "/latest/" not in url


def test_la_versione_non_porta_la_v():
    assert manifesto()["version"] == "1.0.3"


def test_la_data_e_le_note_finiscono_nel_manifesto():
    m = manifesto()
    assert m["pub_date"] == QUANDO
    assert m["notes"] == "Note."


def test_una_firma_vuota_e_un_errore_non_un_manifesto_senza_firma():
    """Un manifesto con `signature: ""` viene pubblicato senza lamentele e
    fallisce solo sul Mac di chi si aggiorna."""
    with pytest.raises(ValueError):
        manifesto(firma="   \n")


def test_una_versione_non_semver_e_un_errore():
    with pytest.raises(ValueError):
        manifesto(versione="v1.0.3")
    with pytest.raises(ValueError):
        manifesto(versione="1.0")


def test_base_url_serve_la_verifica_locale():
    """Il Task 6 serve gli artefatti da un server statico: stessa funzione,
    stessa struttura, altro host."""
    url = manifesto(base_url="http://127.0.0.1:8787")["platforms"]["darwin-aarch64"]["url"]
    assert url == "http://127.0.0.1:8787/Cratory.app.tar.gz"
```

- [ ] **Step 2: Eseguirli e vederli fallire**

```bash
cd backend && source .venv/bin/activate && python -m pytest tests/test_manifest_updater.py -v
```

Atteso: FAIL — il file `pubblica.py` non esiste.

- [ ] **Step 3: Scrivere `pubblica.py`**

Creare `src-tauri/scripts/pubblica.py`:

```python
"""Pubblica una versione: manifesto dell'updater e release su GitHub.

Separato da `assembla.py` di proposito. Costruire e' ripetibile e innocuo;
pubblicare no: crea un artefatto pubblico che qualcun altro scarichera' e che
le app installate useranno per aggiornarsi da sole. Sono due gesti.

Uso:
    python3 src-tauri/scripts/pubblica.py --note-file NOTE.md
    python3 src-tauri/scripts/pubblica.py --note-file NOTE.md --dry-run
    python3 src-tauri/scripts/pubblica.py --note-file NOTE.md --dry-run \\
        --base-url http://127.0.0.1:8787 --manifest-out /tmp/prova/latest.json

Prima di pubblicare pretende che: l'albero di lavoro sia pulito, `VERSION` e
`frontend/package.json` dicano lo stesso numero, HEAD porti il tag `vX.Y.Z` di
quel numero, i tre artefatti esistano, e `gh` sia installato e autenticato.
"""
from __future__ import annotations

import argparse
import datetime
import json
import re
import subprocess
import sys
from pathlib import Path

RADICE = Path(__file__).resolve().parent.parent.parent
BUNDLE = RADICE / "src-tauri" / "target" / "release" / "bundle"
ASSET = "Cratory.app.tar.gz"
REPO = "lucadenegri-dev/Cratory"
PIATTAFORMA = "darwin-aarch64"  # l'unico target che questo progetto costruisce


def costruisci_manifest(
    versione: str, note: str, firma: str, pub_date: str, base_url: str | None = None
) -> dict:
    """Il JSON che l'updater legge.

    `base_url` esiste per la verifica locale (un server statico invece di
    GitHub): la struttura del manifesto e' la stessa, cambia solo da dove si
    scarica, e cosi' quella che si prova e' la funzione vera.
    """
    if not re.fullmatch(r"\d+\.\d+\.\d+", versione):
        raise ValueError(f"versione non semver, e senza `v` iniziale: {versione!r}")
    if not firma.strip():
        raise ValueError(
            "firma vuota: un manifesto con `signature: \"\"` si pubblica senza "
            "lamentele e fallisce solo sul Mac di chi si aggiorna"
        )
    url = (
        f"{base_url.rstrip('/')}/{ASSET}"
        if base_url
        else f"https://github.com/{REPO}/releases/download/v{versione}/{ASSET}"
    )
    return {
        "version": versione,
        "notes": note,
        "pub_date": pub_date,
        "platforms": {PIATTAFORMA: {"signature": firma.strip(), "url": url}},
    }


def _git(*argomenti: str) -> str:
    return subprocess.run(
        ["git", *argomenti], cwd=RADICE, capture_output=True, text=True, check=True
    ).stdout.strip()


def _versione() -> str:
    dal_file = (RADICE / "VERSION").read_text().strip()
    da_npm = json.loads((RADICE / "frontend" / "package.json").read_text())["version"]
    if dal_file != da_npm:
        raise SystemExit(f"VERSION dice {dal_file}, package.json dice {da_npm}")
    return dal_file


def _controlli(versione: str) -> None:
    if _git("status", "--porcelain"):
        raise SystemExit("albero di lavoro sporco: si pubblica solo cio' che e' committato")
    tag = f"v{versione}"
    if tag not in _git("tag", "--points-at", "HEAD").splitlines():
        raise SystemExit(f"HEAD non porta il tag {tag}")
    if subprocess.run(["which", "gh"], capture_output=True).returncode != 0:
        raise SystemExit("gh non e' installato: serve per creare la release")


def _artefatti(versione: str) -> tuple[Path, Path, Path]:
    tar = BUNDLE / "macos" / ASSET
    sig = BUNDLE / "macos" / f"{ASSET}.sig"
    dmg = BUNDLE / "dmg" / f"Cratory_{versione}_aarch64.dmg"
    for p in (tar, sig, dmg):
        if not p.is_file():
            raise SystemExit(f"manca {p} -- ricostruire con assembla.py")
    return tar, sig, dmg


def main() -> None:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--note-file", required=True, type=Path)
    parser.add_argument("--dry-run", action="store_true")
    parser.add_argument("--base-url", default=None)
    parser.add_argument("--manifest-out", type=Path, default=None)
    args = parser.parse_args()

    versione = _versione()
    if not args.dry_run:
        _controlli(versione)
    tar, sig, dmg = _artefatti(versione)
    note = args.note_file.read_text().strip()

    manifest = costruisci_manifest(
        versione=versione,
        note=note,
        firma=sig.read_text(),
        pub_date=datetime.datetime.now(datetime.timezone.utc)
        .replace(microsecond=0)
        .isoformat()
        .replace("+00:00", "Z"),
        base_url=args.base_url,
    )
    destinazione = args.manifest_out or (BUNDLE / "macos" / "latest.json")
    destinazione.parent.mkdir(parents=True, exist_ok=True)
    destinazione.write_text(json.dumps(manifest, indent=2) + "\n")
    print(f"manifesto scritto in {destinazione}")

    if args.dry_run:
        print(json.dumps(manifest, indent=2))
        return

    tag = f"v{versione}"
    esiste = subprocess.run(
        ["gh", "release", "view", tag], cwd=RADICE, capture_output=True
    ).returncode == 0
    allegati = [str(dmg), str(tar), str(sig), str(destinazione)]
    if esiste:
        subprocess.run(
            ["gh", "release", "upload", tag, "--clobber", *allegati], cwd=RADICE, check=True
        )
    else:
        subprocess.run(
            ["gh", "release", "create", tag, "--title", tag,
             "--notes-file", str(args.note_file), *allegati],
            cwd=RADICE, check=True,
        )
    print(f"release {tag} pubblicata")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Rieseguire i test**

```bash
cd backend && source .venv/bin/activate && python -m pytest tests/test_manifest_updater.py -v
```

Atteso: 7 passed. Poi provarli: togliere il `raise ValueError` sulla firma vuota e verificare che *"una firma vuota è un errore"* fallisca. Rimettere.

- [ ] **Step 5: Commit**

```bash
git add src-tauri/scripts/pubblica.py backend/tests/test_manifest_updater.py
git commit -m "feat(updater): manifesto latest.json e script di pubblicazione"
```

---

### Task 6: La verifica sul campo, con due bundle veri

Nessun test automatico può provare questa catena: due processi, un bundle sostituito sotto un interprete vivo, e Gatekeeper. Si prova eseguendola. **Costo: due build complete, lente perché ognuna ricostruisce tutto da zero.**

**Files:**
- Modify: `src-tauri/scripts/assembla.py` (un varco per la configurazione di prova)

- [ ] **Step 1: Il varco nella build**

In `assembla.py`, dove si compone il comando `npm run tauri:build`, aggiungere gli argomenti extra se la variabile c'è:

```python
    # Varco per la verifica dell'updater: `tauri build --config <file>` fonde
    # un frammento di configurazione sopra quello committato, cosi' si puo'
    # puntare l'updater a un server locale senza toccare (e rischiare di
    # committare) tauri.conf.json. Vuota in ogni build normale.
    extra = os.environ.get("CRATORY_TAURI_EXTRA_CONFIG", "").strip()
    comando = ["npm", "run", "tauri:build"]
    if extra:
        comando += ["--", "--config", extra]
```

- [ ] **Step 2: Il frammento di configurazione locale**

```bash
mkdir -p /tmp/cratory-updater && cat > /tmp/cratory-updater/locale.json <<'EOF'
{
  "plugins": {
    "updater": {
      "endpoints": ["http://127.0.0.1:8787/latest.json"],
      "dangerousInsecureTransportProtocol": true
    }
  }
}
EOF
```

Questo file **non entra nel repository**: `/tmp` è fuori dall'albero apposta.

- [ ] **Step 3: Costruire la 1.0.3 (la versione "vecchia")**

Con `VERSION` e `frontend/package.json` a `1.0.3`:

```bash
export PATH="$HOME/.cargo/bin:$PATH"
export TAURI_SIGNING_PRIVATE_KEY="$(cat ~/.tauri/cratory.key)"
export TAURI_SIGNING_PRIVATE_KEY_PASSWORD="<la password>"
export CRATORY_TAURI_EXTRA_CONFIG=/tmp/cratory-updater/locale.json
python3 src-tauri/scripts/assembla.py
cp src-tauri/target/release/bundle/dmg/Cratory_1.0.3_aarch64.dmg /tmp/cratory-updater/
```

- [ ] **Step 4: Costruire la 1.0.4 (la versione "nuova") e servirla**

Portare `VERSION` e `frontend/package.json` a `1.0.4`, ricostruire con gli stessi comandi, poi:

```bash
cp src-tauri/target/release/bundle/macos/Cratory.app.tar.gz /tmp/cratory-updater/
echo "Prova dell'updater." > /tmp/cratory-updater/note.md
python3 src-tauri/scripts/pubblica.py --note-file /tmp/cratory-updater/note.md --dry-run \
  --base-url http://127.0.0.1:8787 --manifest-out /tmp/cratory-updater/latest.json
cd /tmp/cratory-updater && python3 -m http.server 8787
```

- [ ] **Step 5: Installare la 1.0.3 come la installerebbe chiunque**

Aprire `/tmp/cratory-updater/Cratory_1.0.3_aarch64.dmg`, trascinare in Applicazioni, aprire, e fare il giro **Impostazioni di Sistema → Privacy e sicurezza → Apri comunque**. Serve che sia proprio questo il percorso, quarantena compresa: è la condizione di partenza reale.

- [ ] **Step 6: Eseguire l'aggiornamento e guardare tutto**

Da Cratory 1.0.3: Impostazioni → Versione. Verificare in ordine:

1. compare la 1.0.4 con le note (il controllo all'avvio ha già acceso il punto accanto a IMPOSTAZIONI);
2. il clic apre la conferma, e **finché non si conferma non parte alcun download**;
3. i MB avanzano, e poi appare la frase dell'installazione;
4. l'app si chiude e riparte da sola;
5. la versione mostrata è **1.0.4**;
6. `ps aux | grep -c "[u]vicorn"` → nessun orfano sulla 8000, e `lsof -nP -iTCP:8000 -sTCP:LISTEN` mostra solo il processo nuovo;
7. i dati sono intatti: `ls ~/Library/Application\ Support/com.cratory.app/`;
8. **la domanda vera:** l'app riparte senza il dialogo *"Cratory" Not Opened*, sì o no? Annotare la risposta: decide cosa scriverà il README nel Task 7.

- [ ] **Step 7: Provare anche la via che fallisce**

Con l'app installata in una cartella non scrivibile (`sudo chown root /Applications/Cratory.app/..` non è necessario: basta copiare il `.app` in una cartella creata con `chmod 555`), premere "Scarica e installa" e verificare che **fallisca subito**, con la frase dei permessi e senza aver scaricato niente.

- [ ] **Step 8: Riportare `VERSION` e `package.json` a 1.0.3, e commit**

```bash
git add src-tauri/scripts/assembla.py VERSION frontend/package.json
git commit -m "chore(updater): varco per la configurazione di prova e versione 1.0.3"
```

---

### Task 7: La documentazione, scritta dopo aver visto

**Files:**
- Modify: `README.md` (sezioni *Releases* e *Opening it on another Mac*)
- Modify: `docs/ROADMAP.md`
- Modify: `docs/ARCHITECTURE.md`
- Modify: `docs/DEPENDENCIES.md`
- Modify: `PROGRESS.md`

- [ ] **Step 1: README, sezione *Releases***

Riscrivere i passi di pubblicazione: bump di `VERSION` e `package.json`, tag, `export TAURI_SIGNING_PRIVATE_KEY=...`, `assembla.py`, `pubblica.py --note-file`. Dire che la release porta quattro allegati e a cosa serve ciascuno. Sostituire il paragrafo che oggi afferma «**Nothing is downloaded or installed automatically.** There is no auto-updater yet…»: adesso l'app controlla all'avvio, e scarica e installa **su conferma**. Aggiungere l'avvertenza sulla chiave privata: perderla significa che nessuna app già installata potrà più aggiornarsi.

- [ ] **Step 2: README, sezione *Opening it on another Mac***

La frase «Every new version needs the same four steps again» resta vera **per chi installa a mano**. Aggiungere cosa succede a chi si aggiorna dall'app, scrivendo quello che il Task 6 ha osservato — e nient'altro. Se il dialogo non compare, dirlo e dire perché (il bundle sostituito dall'app non passa dalla quarantena). Se compare, dirlo altrettanto chiaramente: l'updater risparmia il download e il trascinamento, non l'approvazione.

- [ ] **Step 3: `docs/ROADMAP.md`**

La voce *Release (2026-08-22)* dichiara che l'updater non c'è e perché. Non cancellarla: le ragioni erano giuste allora. Aggiungere una voce nuova datata 2026-08-23 che dica cosa esiste ora, cosa ha mostrato la verifica sul campo, e cosa resta fuori (firma Apple, aggiornamenti differenziali, Windows e Linux).

- [ ] **Step 4: `docs/ARCHITECTURE.md` e `docs/DEPENDENCIES.md`**

Nella sezione del guscio desktop: il modulo `aggiornamento.rs`, i tre comandi, e **l'ordine** della sequenza col motivo (il backend gira da dentro il bundle che si sostituisce). In DEPENDENCIES: `tauri-plugin-updater`, `@tauri-apps/api`, la chiave minisign come segreto di build, e `gh` come strumento richiesto solo per pubblicare.

- [ ] **Step 5: `PROGRESS.md`**

Una voce di sintesi, nel formato delle altre.

- [ ] **Step 6: Verifica finale di tutto**

```bash
cd backend && source .venv/bin/activate && python -m pytest tests
cd ../frontend && npm run lint && npm run test:unit && npm run build
cd ../src-tauri && cargo test
```

Atteso: tutto verde. Nessuna delle tre suite deve essere saltata.

- [ ] **Step 7: Commit**

```bash
git add README.md docs PROGRESS.md
git commit -m "docs: l'updater esiste, e cosa cambia per chi installa e per chi aggiorna"
```
