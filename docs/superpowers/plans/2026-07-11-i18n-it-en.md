# i18n IT/EN Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cratory bilingue IT/EN — UI frontend, errori backend, testi deterministici e output AI seguono un'impostazione lingua persistente.

**Architecture:** Dizionario TypeScript fatto in casa (EN fonte di verità, IT tipizzato contro EN) con provider React; backend language-agnostic sugli errori (codici stabili tradotti dal frontend); frasi generate deterministiche e prompt AI parametrizzati sulla lingua letta da `AppState`. Spec: `docs/superpowers/specs/2026-07-11-i18n-it-en-design.md`.

**Tech Stack:** Next.js 16 (App Router, React 19, client components), FastAPI + SQLAlchemy + Pydantic, pytest.

## Global Constraints

- Branch di lavoro: `i18n-it-en` (worktree `.claude/worktrees/i18n-it-en`).
- Lingua base nel codice: **inglese**; l'italiano vive solo in `it.ts` (frontend) e nei cataloghi `lang` backend. Default runtime: `"it"`.
- Valori lingua: letterali `"it" | "en"`, chiave `AppState` = `"language"`, chiave localStorage = `"cratory-lang"`.
- Commit **senza** riga `Co-Authored-By` (preferenza utente).
- Next 16: leggere `frontend/CLAUDE.md`; gotcha noto: gli spazi JSX a fine riga spariscono → usare `{" "}` esplicito quando il testo va a capo.
- Comandi test: backend `cd backend && .venv/bin/python -m pytest tests -q`; frontend `cd frontend && npm run build` e `npm run lint`.
- Nei task di migrazione UI non cambiare markup/classi: solo sostituzione di stringhe.
- Non toccare i file di altre feature: prima di ogni commit `git status` e stage esplicito dei soli file del task.

---

### Task 1: Backend — endpoint lingua (`GET/PUT /api/settings/language`)

**Files:**
- Create: `backend/app/routers/settings.py`
- Create: `backend/tests/test_settings_router.py`
- Modify: `backend/app/services/app_state.py` (helper `get_language`)
- Modify: `backend/app/main.py` (registrazione router, vicino alle altre `include_router`)

**Interfaces:**
- Consumes: `get_state(db, key) -> str | None`, `set_state(db, key, value)` da `app/services/app_state.py`; `get_db` da `app/db.py`.
- Produces: `GET /api/settings/language -> {"language": "it"|"en"}`; `PUT /api/settings/language` body `{"language": "it"|"en"}` (422 su altri valori); helper `get_language(db: Session) -> str` (ritorna sempre `"it"` o `"en"`, default `"it"`) usato dai task 13–14.

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# backend/tests/test_settings_router.py
"""Endpoint lingua: GET/PUT /api/settings/language su AppState (chiave `language`)."""
from fastapi.testclient import TestClient
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from sqlalchemy.pool import StaticPool

from app.db import Base, get_db
from app.main import app

client = TestClient(app)


def _override_db():
    engine = create_engine("sqlite://", connect_args={"check_same_thread": False},
                           poolclass=StaticPool)
    Base.metadata.create_all(engine)
    factory = sessionmaker(bind=engine, expire_on_commit=False)

    def _get_db():
        db = factory()
        try:
            yield db
        finally:
            db.close()

    return _get_db


def setup_function():
    app.dependency_overrides[get_db] = _override_db()


def teardown_function():
    app.dependency_overrides.clear()


def test_get_default_it():
    r = client.get("/api/settings/language")
    assert r.status_code == 200
    assert r.json() == {"language": "it"}


def test_put_e_get_en():
    r = client.put("/api/settings/language", json={"language": "en"})
    assert r.status_code == 200
    assert r.json() == {"language": "en"}
    # nuova sessione, stessa engine StaticPool: il valore deve persistere
    assert client.get("/api/settings/language").json() == {"language": "en"}


def test_put_valore_invalido_422():
    r = client.put("/api/settings/language", json={"language": "fr"})
    assert r.status_code == 422


def test_get_language_helper(db):
    from app.services.app_state import get_language, set_state
    assert get_language(db) == "it"
    set_state(db, "language", "en")
    assert get_language(db) == "en"
    set_state(db, "language", "garbage")
    assert get_language(db) == "it"
```

- [ ] **Step 2: Verificare che falliscano**

Run: `cd backend && .venv/bin/python -m pytest tests/test_settings_router.py -v`
Expected: FAIL/ERROR con `404` sugli endpoint (router non esiste) e `ImportError: cannot import name 'get_language'`.

- [ ] **Step 3: Implementazione minima**

```python
# backend/app/routers/settings.py
"""Impostazioni utente persistite in AppState (mono-utente, niente tabella dedicata)."""
from typing import Literal

from fastapi import APIRouter, Depends
from pydantic import BaseModel
from sqlalchemy.orm import Session

from app.db import get_db
from app.services.app_state import LANGUAGE_KEY, get_language, set_state

router = APIRouter(prefix="/api/settings", tags=["settings"])


class LanguageSetting(BaseModel):
    language: Literal["it", "en"]


@router.get("/language", response_model=LanguageSetting)
def read_language(db: Session = Depends(get_db)):
    return LanguageSetting(language=get_language(db))


@router.put("/language", response_model=LanguageSetting)
def write_language(req: LanguageSetting, db: Session = Depends(get_db)):
    set_state(db, LANGUAGE_KEY, req.language)
    return req
```

In `backend/app/services/app_state.py` aggiungere in coda:

```python
LANGUAGE_KEY = "language"
DEFAULT_LANGUAGE = "it"


def get_language(db: Session) -> str:
    """Lingua dell'app ("it" | "en"): valori sconosciuti degradano al default."""
    value = get_state(db, LANGUAGE_KEY)
    return value if value in ("it", "en") else DEFAULT_LANGUAGE
```

In `backend/app/main.py`: aggiungere `settings` all'import dei router e
`app.include_router(settings.router)` dopo `app.include_router(soundcloud.router)`.
Attenzione: esiste già `from app.core.config import settings`? Verificare in
`main.py`; se sì importare il modulo router con alias:
`from app.routers import settings as settings_router` e registrare
`settings_router.router`.

- [ ] **Step 4: Verificare che passino**

Run: `cd backend && .venv/bin/python -m pytest tests/test_settings_router.py tests/test_app_state.py -v`
Expected: PASS tutti.

- [ ] **Step 5: Aggiornare docs/API.md**

Aggiungere sotto una sezione "Settings" (in coda al file, stesso formato delle
altre voci): `GET /api/settings/language` e `PUT /api/settings/language` con
body/response `{"language": "it"|"en"}`.

- [ ] **Step 6: Commit**

```bash
git add backend/app/routers/settings.py backend/app/services/app_state.py backend/app/main.py backend/tests/test_settings_router.py docs/API.md
git commit -m "feat(i18n): endpoint GET/PUT /api/settings/language su AppState"
```

---

### Task 2: Frontend — modulo `lib/i18n` (dizionari + provider) e wiring nel layout

**Files:**
- Create: `frontend/lib/i18n/en.ts`
- Create: `frontend/lib/i18n/it.ts`
- Create: `frontend/lib/i18n/runtime.ts`
- Create: `frontend/lib/i18n/index.tsx`
- Modify: `frontend/app/layout.tsx`

**Interfaces:**
- Consumes: `apiGet`, `apiPut` da `frontend/lib/api.ts`.
- Produces: `useT(): Dictionary` (dizionario della lingua attiva), `useI18n(): { lang, setLang, t }`, `getCurrentLanguage(): "it"|"en"`, `translateApiError(code, params, fallback): string`. Tipo `Dictionary = typeof en`. Tutti i task 3–10 consumano `useT`/`useI18n`; il task 11 consuma `translateApiError`.

- [ ] **Step 1: Creare `en.ts` con i primi namespace**

```ts
// frontend/lib/i18n/en.ts
// Fonte di verità delle chiavi i18n. NIENTE `as const`: i valori devono
// restare `string`/funzioni così `it.ts` può tipizzarsi con `typeof en`.
export const en = {
  common: {
    loading: "Loading…",
    save: "Save",
    cancel: "Cancel",
    close: "Close",
    confirm: "Confirm",
    delete: "Delete",
    search: "Search",
    all: "All",
    none: "None",
    error: "Error",
    retry: "Retry",
  },
  nav: {
    tagline: "DJ set workbench",
    groupDiscover: "Discover",
    groupCollect: "Collect",
    groupPlay: "Play",
    dashboard: "Dashboard",
    discovery: "Discovery",
    shazam: "Shazam",
    library: "Library",
    playlists: "Playlists",
    labels: "Labels",
    downloads: "Downloads",
    sets: "Sets",
    transitions: "Transitions",
    settings: "Settings",
    index: "Index",
    indexStarting: "Starting…",
    indexStarted: "Started",
    toggleTheme: "Toggle theme",
  },
  settings: {
    languageLabel: "Language",
    languageIt: "Italiano",
    languageEn: "English",
  },
  errors: {} as Record<string, string | ((p: Record<string, unknown>) => string)>,
};

export type Dictionary = typeof en;
```

(`errors` parte vuoto e si riempie nel Task 11.)

- [ ] **Step 2: Creare `it.ts`**

```ts
// frontend/lib/i18n/it.ts
// Traduzione italiana: `Dictionary` (= typeof en) garantisce a compile-time
// che ogni chiave esista e nessuna sia di troppo.
import type { Dictionary } from "./en";

export const it: Dictionary = {
  common: {
    loading: "Caricamento…",
    save: "Salva",
    cancel: "Annulla",
    close: "Chiudi",
    confirm: "Conferma",
    delete: "Elimina",
    search: "Cerca",
    all: "Tutte",
    none: "Nessuna",
    error: "Errore",
    retry: "Riprova",
  },
  nav: {
    tagline: "Workbench per DJ set",
    groupDiscover: "Scopri",
    groupCollect: "Colleziona",
    groupPlay: "Suona",
    dashboard: "Dashboard",
    discovery: "Discovery",
    shazam: "Shazam",
    library: "Libreria",
    playlists: "Playlists",
    labels: "Etichette",
    downloads: "Download",
    sets: "Set",
    transitions: "Transizioni",
    settings: "Impostazioni",
    index: "Indicizza",
    indexStarting: "Avvio…",
    indexStarted: "Avviata",
    toggleTheme: "Cambia tema",
  },
  settings: {
    languageLabel: "Lingua",
    languageIt: "Italiano",
    languageEn: "English",
  },
  errors: {},
};
```

- [ ] **Step 3: Creare `runtime.ts` (stato lingua fuori da React)**

Vive in un file senza import da `lib/api.ts`, così `lib/api.ts` potrà
importarlo (Task 11) senza cicli di modulo:

```ts
// frontend/lib/i18n/runtime.ts
// Stato lingua accessibile fuori da React (es. lib/api.ts). Il provider in
// index.tsx lo tiene allineato al context. NIENTE import da lib/api.ts qui.
import { en, type Dictionary } from "./en";
import { it } from "./it";

export type Language = "it" | "en";

export const STORAGE_KEY = "cratory-lang";
export const DICTIONARIES: Record<Language, Dictionary> = { it, en };

let currentLanguage: Language = "it";

export function getCurrentLanguage(): Language {
  return currentLanguage;
}

export function setCurrentLanguage(next: Language): void {
  currentLanguage = next;
}

/** Traduce un codice errore API nella lingua attiva; `fallback` se il codice è ignoto. */
export function translateApiError(
  code: string,
  params: Record<string, unknown>,
  fallback: string,
): string {
  const entry = DICTIONARIES[currentLanguage].errors[code];
  if (typeof entry === "string") return entry;
  if (typeof entry === "function") return entry(params);
  return fallback;
}
```

- [ ] **Step 4: Creare il provider**

```tsx
// frontend/lib/i18n/index.tsx
"use client";

import { createContext, useContext, useEffect, useState, type ReactNode } from "react";
import { apiGet, apiPut } from "@/lib/api";
import { type Dictionary } from "./en";
import { it } from "./it";
import { DICTIONARIES, STORAGE_KEY, setCurrentLanguage, type Language } from "./runtime";

export type { Dictionary, Language };
export { getCurrentLanguage, translateApiError } from "./runtime";

const I18nContext = createContext<{
  lang: Language;
  t: Dictionary;
  setLang: (next: Language) => void;
}>({ lang: "it", t: it, setLang: () => {} });

export function I18nProvider({ children }: { children: ReactNode }) {
  const [lang, setLangState] = useState<Language>("it");

  useEffect(() => {
    /* localStorage evita il flash; il backend resta la fonte persistente. */
    const stored = localStorage.getItem(STORAGE_KEY);
    const initial: Language = stored === "en" ? "en" : "it";
    setCurrentLanguage(initial);
    setLangState(initial);
    document.documentElement.lang = initial;
    apiGet<{ language: Language }>("/api/settings/language")
      .then(({ language }) => {
        if (language !== initial) {
          setCurrentLanguage(language);
          setLangState(language);
          localStorage.setItem(STORAGE_KEY, language);
          document.documentElement.lang = language;
        }
      })
      .catch(() => undefined); // backend giù: si resta sulla lingua locale
  }, []);

  const setLang = (next: Language) => {
    setCurrentLanguage(next);
    setLangState(next);
    localStorage.setItem(STORAGE_KEY, next);
    document.documentElement.lang = next;
    apiPut("/api/settings/language", { language: next }).catch(() => undefined);
  };

  return (
    <I18nContext.Provider value={{ lang, t: DICTIONARIES[lang], setLang }}>
      {children}
    </I18nContext.Provider>
  );
}

export function useT(): Dictionary {
  return useContext(I18nContext).t;
}

export function useI18n() {
  return useContext(I18nContext);
}
```

- [ ] **Step 5: Wiring nel layout**

In `frontend/app/layout.tsx`:
- `import { I18nProvider } from "@/lib/i18n";`
- avvolgere: `<I18nProvider><EditorialShell>{children}</EditorialShell></I18nProvider>`
- estendere `NO_FOUC` per la lingua (evita flash del `lang` attribute):

```ts
const NO_FOUC = `(function(){try{var t=localStorage.getItem('cratory-theme');if(t==='paper'){document.documentElement.setAttribute('data-theme','paper');}var l=localStorage.getItem('cratory-lang');if(l==='en'){document.documentElement.lang='en';}}catch(e){}})();`;
```

`<html lang="it">` resta come default server-side (il provider lo aggiorna al mount).

- [ ] **Step 6: Verifica build**

Run: `cd frontend && npm run build`
Expected: build OK, nessun errore TypeScript.

- [ ] **Step 7: Commit**

```bash
git add frontend/lib/i18n frontend/app/layout.tsx
git commit -m "feat(i18n): dizionari en/it tipizzati, I18nProvider e wiring nel layout"
```

---

### Task 3: Selettore lingua in Impostazioni

**Files:**
- Modify: `frontend/app/settings/page.tsx`

**Interfaces:**
- Consumes: `useI18n()` dal Task 2 (`lang`, `setLang`, `t`).
- Produces: UI per cambiare lingua (nessuna API nuova: `setLang` fa già PUT + localStorage).

- [ ] **Step 1: Aggiungere la sezione lingua**

Leggere `frontend/app/settings/page.tsx` e replicare lo stile delle sezioni
esistenti (stessa struttura di card/righe usata per le altre impostazioni).
Contenuto della sezione, adattando le classi a quelle del file:

```tsx
const { lang, setLang, t } = useI18n();
// ...
<div>
  <span>{t.settings.languageLabel}</span>
  <div className="flex gap-2">
    <button type="button" onClick={() => setLang("it")} aria-pressed={lang === "it"}>
      {t.settings.languageIt}
    </button>
    <button type="button" onClick={() => setLang("en")} aria-pressed={lang === "en"}>
      {t.settings.languageEn}
    </button>
  </div>
</div>
```

Stile dei bottoni: copiare le classi di un toggle/bottone già presente nella
pagina (coerenza col design system, nessuna classe inventata).

- [ ] **Step 2: Verifica manuale**

Avviare backend (`uvicorn app.main:app --reload --port 8000` nel venv) e
frontend dev server; su /settings cambiare lingua e verificare: 1) il toggle
riflette la scelta, 2) `localStorage["cratory-lang"]` aggiornato,
3) `GET /api/settings/language` risponde col nuovo valore dopo reload.

- [ ] **Step 3: Build + commit**

```bash
cd frontend && npm run build && npm run lint
git add frontend/app/settings/page.tsx
git commit -m "feat(i18n): selettore lingua IT/EN in Impostazioni"
```

---

### Procedura comune ai task 4–10 (migrazione stringhe UI)

Ogni task di migrazione segue ESATTAMENTE questi passi (ripetuti per riferimento
in ogni task; l'esecutore di un singolo task legge solo questo blocco + il suo task):

1. Per ogni file del blocco: individuare TUTTE le stringhe italiane user-visible
   (JSX text, attributi `aria-label`/`title`/`placeholder`, stringhe in
   costanti/oggetti tipo label di select, messaggi di stato, `alt`).
   Aiuto: `grep -nE '"[^"]*[àèéìòù][^"]*"|Nessun|Carica|Salva|Annulla|Errore' <file>`
   — ma la fonte di verità è la lettura del file, il grep non cattura tutto.
2. Aggiungere le chiavi al namespace del blocco in `en.ts` E `it.ts` nello
   stesso commit (il type-check fallisce se una delle due manca).
   Naming: camelCase descrittivo (`emptyState`, `importButton`, `bpmRange`).
   Stringhe con parametri = funzioni: `tracksCount: (n: number) => n === 1 ? "1 track" : n + " tracks"`.
3. Nei componenti: `const t = useT();` (il componente deve essere/diventare
   client component — verificare la direttiva `"use client"`; se un file è
   server component senza interattività, valutare se le stringhe possono
   restare in un sotto-componente client già esistente, altrimenti aggiungere
   `"use client"` è accettabile: l'app è dietro provider client comunque).
4. Sostituire le stringhe con `t.<ns>.<chiave>`. NON cambiare markup né classi.
   Occhio agli spazi JSX di Next 16: se il testo andava a capo, usare `{" "}`.
5. `cd frontend && npm run build && npm run lint` → devono passare.
6. Verifica visiva rapida col dev server in entrambe le lingue sulle pagine toccate.
7. Commit dei soli file del blocco + `en.ts`/`it.ts`:
   `git commit -m "feat(i18n): traduzione <blocco>"`.

Regole di contenuto:
- Nomi propri e termini di dominio invariati: Cratory, Rekordbox, Shazam,
  Spotify, SoundCloud, slskd, Camelot, BPM, key, Discovery, Dashboard.
- Testo EN: conciso, sentence case (stile "editorial archive" del design system).
- Commenti in italiano nel codice NON si toccano (non sono UI).

---

### Task 4: Migrazione shell e navigazione

**Files:**
- Modify: `frontend/components/index-nav.tsx`, `frontend/components/theme-toggle.tsx`, `frontend/components/editorial-shell.tsx`, `frontend/components/page-layout.tsx`, `frontend/components/ui.tsx`, `frontend/components/jobs-provider.tsx`, `frontend/components/clock.tsx`
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts` (namespace `nav`, `jobs`, ampliare `common`)

**Interfaces:**
- Consumes: `useT()` dal Task 2. Le chiavi `nav.*` esistono già dal Task 2.
- Produces: shell completamente bilingue; namespace `jobs` per la barra job.

- [ ] **Step 1: Seguire la "Procedura comune" (sopra) su ogni file del blocco.**

Esempio completo su `index-nav.tsx` (le altre sostituzioni seguono lo stesso schema):

```tsx
// NAV_GROUPS diventa una funzione del dizionario (le label non sono più costanti modulo):
const navGroups = (t: Dictionary) => [
  { title: null, items: [{ href: "/", label: t.nav.dashboard }] },
  {
    title: t.nav.groupDiscover,
    items: [
      { href: "/discovery", label: t.nav.discovery },
      { href: "/shazam", label: t.nav.shazam },
    ],
  },
  {
    title: t.nav.groupCollect,
    items: [
      { href: "/library", label: t.nav.library },
      { href: "/playlists", label: t.nav.playlists },
      { href: "/labels", label: t.nav.labels },
      { href: "/downloads", label: t.nav.downloads },
    ],
  },
  {
    title: t.nav.groupPlay,
    items: [
      { href: "/sets", label: t.nav.sets },
      { href: "/transitions", label: t.nav.transitions },
    ],
  },
];

// Dentro IndexNav():
const t = useT();
const NAV_GROUPS = navGroups(t);
const scanLabel = scan === "busy" ? t.nav.indexStarting : scan === "done" ? t.nav.indexStarted : t.nav.index;
// "Workbench per DJ set" -> {t.nav.tagline}
// aria-label="Impostazioni" -> aria-label={t.nav.settings}
// "Impostazioni" (testo link) -> {t.nav.settings}
```

In `theme-toggle.tsx`: `aria-label="Cambia tema"` → `aria-label={t.nav.toggleTheme}`
(aggiungere `const t = useT();` nel componente). Le label "Paper"/"Dark" sono
nomi propri dei temi: restano invariate.

In `jobs-provider.tsx`: le label dei job (es. `label: "Identificazione mix"`)
vanno nel namespace `jobs` (`jobs.shazamIdentify: "Mix identification"` /
`"Identificazione mix"`, ecc. — enumerare leggendo il file). NB: `v.phase`
arriva dal backend e si localizza nel Task 14 — qui non toccarlo.

- [ ] **Step 2: Build + lint**

Run: `cd frontend && npm run build && npm run lint` — Expected: PASS.

- [ ] **Step 3: Verifica visiva in entrambe le lingue (dev server, toggle su /settings).**

- [ ] **Step 4: Commit**

```bash
git add frontend/components frontend/lib/i18n
git commit -m "feat(i18n): traduzione shell, navigazione e barra job"
```

---

### Task 5: Migrazione Dashboard

**Files:**
- Modify: `frontend/app/page.tsx`, `frontend/components/dashboard/figure.tsx`, `frontend/components/dashboard/histogram.tsx`, `frontend/components/dashboard/mini-bars.tsx`, `frontend/components/dashboard/pipeline.tsx`, `frontend/components/dashboard/recent-list.tsx`
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts` (namespace `dashboard`)

**Interfaces:**
- Consumes: `useT()`; procedura comune (vedi sopra, tra Task 3 e Task 4).
- Produces: namespace `dashboard` completo.

- [ ] **Step 1: Applicare la Procedura comune a ogni file del blocco** (namespace `dashboard`, conteggi come funzioni `(n: number) => string`).
- [ ] **Step 2: Build + lint** — `cd frontend && npm run build && npm run lint` → PASS.
- [ ] **Step 3: Verifica visiva IT/EN della dashboard.**
- [ ] **Step 4: Commit** — `git add frontend/app/page.tsx frontend/components/dashboard frontend/lib/i18n && git commit -m "feat(i18n): traduzione dashboard"`

---

### Task 6: Migrazione Libreria e Tracce

**Files:**
- Modify: `frontend/app/library/page.tsx`, `frontend/app/tracks/[id]/page.tsx`, `frontend/components/track-edit-modal.tsx`, `frontend/components/track-state-icons.tsx`, `frontend/components/track-cover.tsx`, `frontend/components/key-badge.tsx`, `frontend/components/auto-link-modal.tsx`, `frontend/components/link-local-file-modal.tsx`
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts` (namespace `library`, `tracks`)

**Interfaces:**
- Consumes: `useT()`; procedura comune (vedi sopra, tra Task 3 e Task 4).
- Produces: namespace `library` e `tracks` completi (filtri, stati traccia, modali di edit/link).

- [ ] **Step 1: Applicare la Procedura comune a ogni file del blocco.** Attenzione alle option di select/filtri (sorgente, stato, ordinamento): ogni option label è una chiave.
- [ ] **Step 2: Build + lint** → PASS.
- [ ] **Step 3: Verifica visiva IT/EN di /library e /tracks/[id].**
- [ ] **Step 4: Commit** — `git commit -m "feat(i18n): traduzione libreria e dettaglio traccia"` (stage: file del blocco + dizionari).

---

### Task 7: Migrazione Playlists (+ import)

**Files:**
- Modify: `frontend/app/playlists/page.tsx`, `frontend/app/playlists/[id]/page.tsx`, `frontend/app/playlists/[id]/expand/page.tsx`, `frontend/app/playlists/import-manual/page.tsx`, `frontend/app/playlists/import-spotify/page.tsx`, `frontend/app/playlists/import-spotify/liked/page.tsx`, `frontend/app/playlists/import-soundcloud/page.tsx`, `frontend/app/playlists/import-soundcloud/likes/page.tsx`, `frontend/components/expand-results.tsx`
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts` (namespace `playlists`)

**Interfaces:**
- Consumes: `useT()`; procedura comune (vedi sopra, tra Task 3 e Task 4).
- Produces: namespace `playlists` completo (liste, dettaglio, espansione, 4 flussi di import).

- [ ] **Step 1: Applicare la Procedura comune a ogni file del blocco.** I flussi import hanno molti messaggi di stato/istruzioni: chiavi per step (`importSpotify.connectHint`, `importSoundcloud.usernameLabel`, …) — sotto-oggetti dentro `playlists`.
- [ ] **Step 2: Build + lint** → PASS.
- [ ] **Step 3: Verifica visiva IT/EN di /playlists e dei flussi import.**
- [ ] **Step 4: Commit** — `git commit -m "feat(i18n): traduzione playlists e flussi import"`.

---

### Task 8: Migrazione Discovery e Shazam

**Files:**
- Modify: `frontend/app/discovery/page.tsx`, `frontend/components/discovery-lead-grid.tsx`, `frontend/components/discovery-tracklist-panel.tsx`, `frontend/app/shazam/page.tsx`, `frontend/app/shazam/[id]/page.tsx`
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts` (namespace `discovery`, `shazam`)

**Interfaces:**
- Consumes: `useT()`; procedura comune (vedi sopra, tra Task 3 e Task 4).
- Produces: namespace `discovery` e `shazam` completi.

- [ ] **Step 1: Applicare la Procedura comune a ogni file del blocco.** "Scava" è il nome del dig Discogs: tradurlo (`dig: "Dig"` / `"Scava"`), è UI non nome proprio.
- [ ] **Step 2: Build + lint** → PASS.
- [ ] **Step 3: Verifica visiva IT/EN di /discovery e /shazam.**
- [ ] **Step 4: Commit** — `git commit -m "feat(i18n): traduzione discovery e shazam"`.

---

### Task 9: Migrazione Set Builder, Sets e Transizioni

**Files:**
- Modify: `frontend/app/set-builder/page.tsx`, `frontend/app/set-builder/guida/page.tsx`, `frontend/app/sets/page.tsx`, `frontend/app/sets/[id]/page.tsx`, `frontend/app/transitions/page.tsx`, `frontend/components/set-arc.tsx`
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts` (namespace `setBuilder`, `sets`, `transitions`)

**Interfaces:**
- Consumes: `useT()`; procedura comune (vedi sopra, tra Task 3 e Task 4).
- Produces: namespace `setBuilder`, `sets`, `transitions` completi. NON toccare `classification_label`/`transition_class_label` (arrivano dal backend; diventano codici nel Task 13).

- [ ] **Step 1: Applicare la Procedura comune a ogni file del blocco.** `guida/page.tsx` è una pagina di testo lungo: chiavi per paragrafo (`guide.intro`, `guide.step1Title`, …) — accettabile che siano stringhe lunghe.
- [ ] **Step 2: Build + lint** → PASS.
- [ ] **Step 3: Verifica visiva IT/EN di /set-builder, /sets, /transitions.**
- [ ] **Step 4: Commit** — `git commit -m "feat(i18n): traduzione set builder, sets e transizioni"`.

---

### Task 10: Migrazione Labels, Downloads e Impostazioni (resto)

**Files:**
- Modify: `frontend/app/labels/page.tsx`, `frontend/app/labels/[label]/page.tsx`, `frontend/app/downloads/page.tsx`, `frontend/components/download-review-modal.tsx`, `frontend/app/settings/page.tsx` (stringhe restanti)
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts` (namespace `labels`, `downloads`, ampliare `settings`)

**Interfaces:**
- Consumes: `useT()`; procedura comune (vedi sopra, tra Task 3 e Task 4).
- Produces: UI frontend 100% bilingue (chiudere il giro: `grep -rnE '[àèéìòù]' frontend/app frontend/components --include='*.tsx' | grep -v '//'` non deve restituire testo UI — solo eventuali commenti).

- [ ] **Step 1: Applicare la Procedura comune a ogni file del blocco.**
- [ ] **Step 2: Sweep finale**: il grep sopra + lettura veloce; ogni residuo UI si sistema qui.
- [ ] **Step 3: Build + lint** → PASS.
- [ ] **Step 4: Verifica visiva IT/EN di /labels, /downloads, /settings.**
- [ ] **Step 5: Commit** — `git commit -m "feat(i18n): traduzione labels, downloads e impostazioni"`.

---

### Task 11: Backend — helper `api_error` + traduzione errori in `lib/api.ts` + migrazione `playlists.py` e `soundcloud.py`

**Files:**
- Create: `backend/app/core/http_errors.py`
- Modify: `backend/app/routers/playlists.py`, `backend/app/routers/soundcloud.py`
- Modify: `frontend/lib/api.ts` (funzione `handle`)
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts` (namespace `errors`)
- Modify: test backend che asseriscono sulle stringhe di errore di questi router (individuarli con `grep -rln "non trovata\|non sincronizzabile\|non configurato\|non valido" backend/tests`)

**Interfaces:**
- Consumes: `translateApiError` dal Task 2.
- Produces: `api_error(status_code: int, code: str, message: str, **params) -> HTTPException` con `detail = {"code", "message", "params"?}`; convenzione codici `snake_case` `<entità>_<problema>` (es. `playlist_not_found`). I test asseriscono su `r.json()["detail"]["code"]`.

- [ ] **Step 1: Test del helper**

```python
# backend/tests/test_http_errors.py
"""api_error: HTTPException con detail strutturato {code, message, params?}."""
from app.core.http_errors import api_error


def test_detail_strutturato():
    exc = api_error(404, "playlist_not_found", "Playlist not found")
    assert exc.status_code == 404
    assert exc.detail == {"code": "playlist_not_found", "message": "Playlist not found"}


def test_params_opzionali():
    exc = api_error(409, "job_running", "Job already running", job="index")
    assert exc.detail["params"] == {"job": "index"}
```

Run: `pytest tests/test_http_errors.py -v` → FAIL (modulo mancante).

- [ ] **Step 2: Implementare il helper**

```python
# backend/app/core/http_errors.py
"""Errori HTTP con codice stabile: il frontend traduce `code` nella lingua
attiva; `message` (inglese) è il fallback leggibile per debug/curl."""
from fastapi import HTTPException


def api_error(status_code: int, code: str, message: str, **params) -> HTTPException:
    detail: dict = {"code": code, "message": message}
    if params:
        detail["params"] = params
    return HTTPException(status_code=status_code, detail=detail)
```

Run: `pytest tests/test_http_errors.py -v` → PASS.

- [ ] **Step 3: Migrare `playlists.py` e `soundcloud.py`**

Sostituire ogni `raise HTTPException(status_code=S, detail="<italiano>")` con
`raise api_error(S, "<code>", "<English message>")`. Mappatura completa dei
due router (verificare a mano che copra tutte le occorrenze di `detail=`):

| Vecchio detail (IT) | code | message (EN) |
|---|---|---|
| Playlist non trovata | `playlist_not_found` | Playlist not found |
| Playlist SoundCloud non sincronizzabile: usa il flusso selettivo dei like o reimporta l'URL. | `soundcloud_playlist_not_syncable` | SoundCloud playlists can't be synced: use the selective likes flow or re-import the URL. |
| Playlist non sincronizzabile da Spotify. | `playlist_not_syncable` | This playlist can't be synced from Spotify. |
| Nessuna traccia riconosciuta nel testo fornito. | `no_tracks_recognized` | No tracks recognized in the given text. |
| Username SoundCloud non configurato (Impostazioni). | `soundcloud_username_missing` | SoundCloud username not configured (Settings). |
| Username SoundCloud non valido. | `soundcloud_username_invalid` | Invalid SoundCloud username. |
| `detail=str(exc)` in `_http_error` (SoundCloudError) | `soundcloud_invalid_url` (per `SoundCloudInvalidUrl`) / `soundcloud_error` | `str(exc)` come message, `params={"reason": str(exc)}` |

Se nei due router compaiono altri `detail=` non in tabella, coniare il codice
con la stessa convenzione e aggiungerlo a dizionari e tabella del commit.

- [ ] **Step 4: Aggiornare `frontend/lib/api.ts`**

```ts
import { translateApiError } from "@/lib/i18n/runtime";

async function handle<T>(res: Response): Promise<T> {
  if (!res.ok) {
    let detail = res.statusText;
    try {
      const body = await res.json();
      const d = body.detail;
      if (d && typeof d === "object" && !Array.isArray(d) && typeof d.code === "string") {
        detail = translateApiError(d.code, d.params ?? {}, d.message ?? res.statusText);
      } else {
        detail = typeof d === "string" ? d : JSON.stringify(d);
      }
    } catch {
      /* keep statusText */
    }
    throw new Error(detail);
  }
  if (res.status === 204) return undefined as T;
  return res.json() as Promise<T>;
}
```

Nota cicli di modulo: `lib/api.ts` importa SOLO da `@/lib/i18n/runtime`
(creato nel Task 2, nessun import da `lib/api.ts` al suo interno) — mai da
`@/lib/i18n` (index.tsx), che importa `apiGet/apiPut` e creerebbe un ciclo.

- [ ] **Step 5: Riempire `errors` nei dizionari**

In `en.ts` (i valori EN = colonna message; con params → funzione):

```ts
errors: {
  playlist_not_found: "Playlist not found",
  soundcloud_playlist_not_syncable: "SoundCloud playlists can't be synced: use the selective likes flow or re-import the URL.",
  playlist_not_syncable: "This playlist can't be synced from Spotify.",
  no_tracks_recognized: "No tracks recognized in the given text.",
  soundcloud_username_missing: "SoundCloud username not configured (Settings).",
  soundcloud_username_invalid: "Invalid SoundCloud username.",
  soundcloud_invalid_url: (p: Record<string, unknown>) => `Invalid SoundCloud URL: ${p.reason ?? ""}`,
  soundcloud_error: (p: Record<string, unknown>) => `SoundCloud error: ${p.reason ?? ""}`,
} as Record<string, string | ((p: Record<string, unknown>) => string)>,
```

e gli equivalenti IT in `it.ts` (riprendere le stringhe italiane originali).

- [ ] **Step 6: Aggiornare i test backend coinvolti**

I test che facevano assert su stringhe italiane di questi router passano a:
`assert r.json()["detail"]["code"] == "playlist_not_found"`.

- [ ] **Step 7: Test + build**

Run: `cd backend && .venv/bin/python -m pytest tests -q` → PASS (tutta la suite).
Run: `cd frontend && npm run build` → PASS.

- [ ] **Step 8: Commit**

```bash
git add backend/app/core/http_errors.py backend/app/routers/playlists.py backend/app/routers/soundcloud.py backend/tests frontend/lib/api.ts frontend/lib/i18n
git commit -m "feat(i18n): codici errore stabili (api_error) su playlists/soundcloud, traduzione in lib/api.ts"
```

---

### Task 12: Backend — sweep `api_error` sui router restanti

**Files:**
- Modify: tutti i router con `detail=` residui: `tracks.py`, `spotify.py`, `downloads.py`, `sets.py`, `dj_sets.py`, `discovery.py`, `labels.py`, `files.py`, `pipeline.py`, `rekordbox.py`, `ai.py`, `transitions.py`, `services.py` (elenco effettivo: `grep -rln 'detail=' backend/app/routers`)
- Modify: `frontend/lib/i18n/en.ts`, `frontend/lib/i18n/it.ts` (namespace `errors`)
- Modify: test backend che asseriscono su stringhe di errore

**Interfaces:**
- Consumes: `api_error` dal Task 11; convenzione codici `<entità>_<problema>`.
- Produces: zero `HTTPException(detail="<stringa italiana>")` nel backend; ogni codice presente in `errors` di entrambi i dizionari.

- [ ] **Step 1: Enumerare** — `grep -rn "detail=" backend/app/routers/` (attesi ~70 residui dopo il Task 11).
- [ ] **Step 2: Migrare router per router** con la stessa procedura/tabella del Task 11 Step 3: codice `snake_case`, message inglese; `str(exc)` dinamici → codice di categoria + `params={"reason": str(exc)}`. Dopo ogni router: aggiungere i codici a `en.ts`/`it.ts` (IT = stringa italiana originale del raise).
- [ ] **Step 3: Aggiornare i test** — `grep -rn "detail" backend/tests | grep -iE "[àèéìòù]|non |già"` per trovare gli assert su stringhe; passare a assert sul `code`.
- [ ] **Step 4: Verifica finale** — `grep -rnE 'detail="[^"]*[àèéìòù]' backend/app` → zero risultati; `pytest tests -q` → PASS; `npm run build` → PASS.
- [ ] **Step 5: Commit** — `git commit -m "feat(i18n): api_error su tutti i router, catalogo errori completo"` (stage: router toccati + test + dizionari).

---

### Task 13: Scoring — `label_it` via, frasi generate parametrizzate sulla lingua

**Files:**
- Modify: `backend/app/services/scoring.py` (`TransitionClassification`, `classify_transition`, `mixing_tip`, `mixing_overview`)
- Modify: `backend/app/serializers.py` (righe 76-78), `backend/app/routers/transitions.py` (riga 19-20), `backend/app/routers/sets.py` (riga ~197, export)
- Modify: `frontend/lib/api.ts` (tipi), `frontend/app/transitions/page.tsx`, `frontend/app/sets/[id]/page.tsx`, `frontend/lib/i18n/en.ts` / `it.ts` (namespace `transitionLabels`)
- Modify: test backend su classificazione/tip (individuarli: `grep -rln "label_it\|tecnicamente sicura\|mixing_tip" backend/tests`)

**Interfaces:**
- Consumes: `get_language(db)` dal Task 1.
- Produces: `classify_transition(from_track, to_track, lang: str = "it")`, `mixing_tip(from_track, to_track, lang: str = "it")`, `mixing_overview(..., lang: str = "it")`; `TransitionClassification` SENZA `label_it` (campi: `label`, `reason`); API: `classification_label`/`transition_class_label` rimossi dalle response, il frontend traduce `classification`/`transition_class` via `t.transitionLabels`.

- [ ] **Step 1: Test prima**

Aggiornare/aggiungere test in `backend/tests` (file esistente sulla
classificazione, altrimenti `test_scoring_i18n.py`):

```python
def test_classify_transition_en(db):
    # due tracce compatibili già usate nei test esistenti di classify_transition
    cls_it = classify_transition(a, b)              # default "it"
    cls_en = classify_transition(a, b, lang="en")
    assert cls_it.label == cls_en.label             # il codice non dipende dalla lingua
    assert cls_it.reason != cls_en.reason
    assert not hasattr(cls_it, "label_it")


def test_mixing_tip_en(db):
    assert mixing_tip(a, b, lang="en") != mixing_tip(a, b, lang="it")
```

Run → FAIL (firma senza `lang`, `label_it` presente).

- [ ] **Step 2: Implementare in `scoring.py`**

Pattern: le stringhe escono da cataloghi per lingua interni al modulo. Esempio
per `classify_transition` (stessa tecnica per `mixing_tip`/`mixing_overview`,
che vanno letti e convertiti frase per frase):

```python
_REASONS = {
    "harmonic_safe": {
        "it": "BPM e tonalità compatibili: mix sicuro",
        "en": "Compatible BPM and key: safe mix",
    },
    "energy_drop": {"it": "calo di energia ({delta:+d})", "en": "energy drop ({delta:+d})"},
    "genre_change": {"it": "cambio di genere", "en": "genre change"},
    "reset": {
        "it": "Stacco netto ({bits}): utile per resettare la pista",
        "en": "Hard cut ({bits}): useful to reset the floor",
    },
    "key_clash": {
        "it": "Beatmatch facile ma tonalità in contrasto: mix breve o maschera con l'EQ",
        "en": "Easy beatmatch but clashing keys: keep the mix short or mask with EQ",
    },
    "creative_jump": {
        "it": "Salto di BPM/tonalità voluto ma azzardato: gestire con cura",
        "en": "Deliberate but risky BPM/key jump: handle with care",
    },
}


def _txt(key: str, lang: str, **fmt) -> str:
    return _REASONS[key][lang if lang in ("it", "en") else "it"].format(**fmt)
```

`TransitionClassification` perde `label_it` (restano `label`, `reason`);
`classify_transition(from_track, to_track, lang: str = "it")` compone `reason`
con `_txt(...)`. `mixing_tip`/`mixing_overview` prendono lo stesso parametro e
lo stesso pattern a catalogo (definire un dict `_TIPS` analogo con TUTTE le
frasi esistenti nelle due lingue; la stringa "apertura" in `sets.py:197`
diventa anch'essa voce di catalogo: it "apertura" / en "opener").

- [ ] **Step 3: Propagare `lang` ai chiamanti**

`serializers.py` e `routers/transitions.py` e `routers/sets.py`: dove chiamano
`classify_transition`/`mixing_tip`, ricavare `lang = get_language(db)` una
volta per request e passarlo. Se il serializer non riceve `db`, aggiungere il
parametro `lang: str = "it"` alla funzione di serializzazione e passarlo dal
router (che ha `db`). Rimuovere `label_it` dalle response
(`classification_label`, `transition_class_label`) e i relativi campi dai tipi
in `frontend/lib/api.ts`.

- [ ] **Step 4: Frontend traduce i label enum**

Dizionari, namespace nuovo:

```ts
transitionLabels: {
  technically_safe: "Technically safe",   // it: "Tecnicamente sicura"
  creative_risk: "Creative risk",         // it: "Azzardo creativo"
  good_reset: "Good reset",               // it: "Reset voluto"
},
```

`app/transitions/page.tsx:153`: `{score.classification_label ?? score.classification}` →
`{t.transitionLabels[score.classification as keyof typeof t.transitionLabels] ?? score.classification}`.
`app/sets/[id]/page.tsx:283`: stesso pattern su `st.transition_class`.
(`title={...reason}` resta: la reason arriva già localizzata dal backend.)

- [ ] **Step 5: Test + build** — `pytest tests -q` PASS; `npm run build` PASS. Verifica visiva /transitions in EN.
- [ ] **Step 6: Commit** — `git commit -m "feat(i18n): scoring bilingue, via label_it (label tradotta dal frontend)"`.

---

### Task 14: AI — prompt, fasi job ed errori parametrici sulla lingua

**Files:**
- Modify: `backend/app/services/ai_agent.py`
- Modify: eventuale chiamante che passa `on_phase` (individuare con `grep -rn "on_phase\|generate_ai_set\|ai_agent\." backend/app/routers backend/app/services`)
- Modify: `backend/tests/test_ai_agent.py` (se asserisce sui prompt/fasi)

**Interfaces:**
- Consumes: `get_language(db)` dal Task 1 (il `db` è già disponibile nella funzione principale di `ai_agent.py`, vedi `_safe_library_context(db)` alla riga ~254).
- Produces: prompt di sistema SENZA "Scrivi SEMPRE in italiano" hardcoded; istruzione lingua appesa a runtime; messaggi `phase(...)` e `AIAgentError` nelle due lingue.

- [ ] **Step 1: Test prima**

In `test_ai_agent.py` (adattare ai mock già presenti nel file — il client LLM
è monkeypatchato; catturare l'argomento `system` passato a `complete_json`):

```python
def test_prompt_lingua_en(...):
    set_state(db, "language", "en")
    # ... generazione con client mock che registra `system`
    assert "ALWAYS write in English" in captured_system
    assert "italiano" not in captured_system
```

Run → FAIL.

- [ ] **Step 2: Implementare**

In `ai_agent.py`:

```python
LANGUAGE_INSTRUCTIONS = {
    "it": "Scrivi SEMPRE in italiano. Rispondi esclusivamente nel formato JSON richiesto.",
    "en": "ALWAYS write in English. Reply exclusively in the requested JSON format.",
}

_PHASES = {
    "candidates": {"it": "Seleziono le tracce candidate", "en": "Selecting candidate tracks"},
    "building":  {"it": "L'AI sta costruendo il set (puo' richiedere un minuto)",
                  "en": "The AI is building the set (may take a minute)"},
    "validating": {"it": "Valido il risultato", "en": "Validating the result"},
}
```

- Rimuovere la riga finale "Scrivi SEMPRE in italiano. Rispondi esclusivamente
  nel formato JSON richiesto." da `SYSTEM_PROMPT` e `CREATIVE_SYSTEM_PROMPT`.
- Nella funzione di generazione: `lang = get_language(db)`;
  `system = base_prompt + "\n" + LANGUAGE_INSTRUCTIONS[lang]`;
  `phase(_PHASES["candidates"][lang])` ecc.
- `AIAgentError("L'AI non ha prodotto tracce valide tra le candidate.")` →
  catalogo a due lingue nello stesso stile (`_ERRORS = {...}`), selezione con `lang`.
- I prompt restano in italiano come "corpo" (sono istruzioni al modello, non
  UI): cambia SOLO l'istruzione di lingua dell'output. Questo è deliberato.

- [ ] **Step 3: Test** — `pytest tests/test_ai_agent.py -q` e poi tutta la suite → PASS.
- [ ] **Step 4: Commit** — `git commit -m "feat(i18n): output AI, fasi job ed errori agente nella lingua selezionata"`.

---

### Task 15: Verifica finale e documentazione

**Files:**
- Modify: `docs/ROADMAP.md` (stato feature), `PROGRESS.md` (voce diario), `docs/ARCHITECTURE.md` (paragrafo breve su i18n: dizionari frontend, codici errore, lang nelle frasi generate/AI)

**Interfaces:**
- Consumes: tutto quanto sopra.
- Produces: branch pronto per merge/PR.

- [ ] **Step 1: Suite completa** — `cd backend && .venv/bin/python -m pytest tests -q` → PASS, zero failure.
- [ ] **Step 2: Frontend** — `cd frontend && npm run build && npm run lint` → PASS.
- [ ] **Step 3: Sweep residui** — `grep -rnE '"[^"]*[àèéìòù]' frontend/app frontend/components --include='*.tsx'` e `grep -rnE 'detail="[^"]*[àèéìòù]' backend/app`: nessuna stringa UI residua (i commenti sono ok).
- [ ] **Step 4: Verifica manuale end-to-end** — dev server: toggle lingua su /settings; girare Dashboard, Library, Playlists (un import), Discovery, Set Builder (se AI configurata: generare un set in EN e verificare narrativa inglese), Transitions, Downloads, Labels, Shazam in ENTRAMBE le lingue; provocare un errore (es. sync di playlist SoundCloud) e verificare messaggio tradotto.
- [ ] **Step 5: Docs** — aggiornare i tre file: ROADMAP (feature fatta), PROGRESS (voce con data 2026-07-11+, cosa e come), ARCHITECTURE (sezione breve "Internazionalizzazione").
- [ ] **Step 6: Commit finale** — `git add docs PROGRESS.md && git commit -m "docs(i18n): stato roadmap, diario e nota architettura"`.
- [ ] **Step 7: Chiusura branch** — usare la skill superpowers:finishing-a-development-branch (merge su master o PR, a scelta dell'utente).
